namespace Xlide.Vbe.Core.Scm;

/// <summary>The sentences the pane and the api route both say, from one place.</summary>
public static class ScmWords
{
    /// <summary>What the rows compare, and which saves reach the folder.</summary>
    public const string Covers =
        "Compares each module's code as the editor holds it with the file at the branch head; "
        + "attribute headers and a form's design are written and compared by the export at save "
        + "and commit, so an attribute-only change reads as clean until then. A save through this "
        + "editor writes the folder. A save from Excel's own window is not seen, and the folder "
        + "catches up at the next save here or at Commit.";
}
