# The xlide api

A local HTTP door into a running xlide session: ask what the editor is doing, drive it by
name, and read what its panels hold. It exists so diagnostics, tests, performance work and
AI agents can act semantically instead of posting mouse messages at measured pixels, and its
route shapes are product-grade because the same door is the intended path for xlide_vscode
and xlide_vbide to talk to each other.

**It ships, and it ships shut.** Every build carries every route here; a release build opens
none of them until someone turns the api on from the agent card (the robot button on the
toolbar). One api, not a debug one and a product one that drift apart.

- The choice is written to `%LOCALAPPDATA%\xlide_vbide\settings.json` as `api.enabled`, and
  it holds across restarts in both directions.
- With the key absent, the build decides: a dev build opens the door, because the harness is
  the reason it exists; a shipped build keeps it shut.
- `verify.ps1` reads the published Release binary and fails if it says it leans open.

**What being on means.** A loopback listener on a random port, and a token written to a file
under your profile. Anything running as you can read that file, and anything holding the
token can read, write and run code in every project the editor can reach. That is the whole
of the security model: local, and as trusted as your own account.

**The DevTools protocol is NOT part of this and never ships.** Dev builds also ask the
browser for `--remote-debugging-port` with `--remote-allow-origins=*`, which takes no
credential at all. It is a harness tool, it is `#if DEBUG`, and the gate fails if a release
binary carries either flag. `devtoolsPort` reads `0` in a shipped build.

## Finding an instance

The api belongs to an ADD-IN SESSION, which exists once per Excel process that has opened
the VBE. Each session writes a discovery file:

```text
%LOCALAPPDATA%\xlide_vbide\xlide-api-{pid}.json
{"api":1,"port":61503,"token":"92a9...","devtoolsPort":61501,"pid":21916,"startedAt":"...",
 "product":"xlide_vbide","host":"excel","agent":"http://127.0.0.1:61503/92a9.../agent"}
```

Every request goes to `http://127.0.0.1:{port}/{token}/{route}`. The port and token are
random per session; the token is the only gate, and a wrong one gets a 404.

**Several Excels.** Two situations look alike and are not:

- **Several workbooks, one Excel process.** The usual case. Excel reuses its process, so
  there is ONE session and one api, and the workbooks are told apart per request with
  `project=` (see the module route). `windows` lists every workbook's panes.
- **Several Excel processes** (`excel /x`, or separate logons). Each process that opens the
  VBE gets its own session, its own discovery file, its own port and token, and its own
  DevTools port. Nothing is shared and nothing collides.

A discovery file outlives a killed Excel, so answering is the only proof of life. Each new
session sweeps files whose process is gone, and clients should probe rather than trust.
`tools\harness\xlide-api.mjs` does all of this: `discover()` lists live instances,
`open({ workbook })` picks one, and it refuses to guess when the choice is ambiguous.

## An agent's first request

The discovery file carries three fields for a caller that knows nothing yet: `product`
(`xlide_vbide`), `host` (which Office application answered - the add-in loads in Word and
PowerPoint as readily as Excel, and an agent about to write VBA needs to know which object
model it is writing against), and `agent`, a ready-to-fetch URL. Its reply is the breadcrumb
trail: identity, how to call, `startHere` steps, and `next` URLs to the route table
(`agent/routes`), runnable recipes (`agent/examples`), the host object model (`model`), the
analyzer's rules (`analyzer`), and the live session (`state`, `doctor`). Everything the rest
of this document teaches a person, those routes teach a program, and the suite holds them to
it: every advertised breadcrumb must resolve, and every route the table calls safe to try
bare must answer as itself.

So the whole hand-off to an agent is one sentence: *read
`%LOCALAPPDATA%\xlide_vbide\xlide-api-*.json`, GET the `agent` URL inside it, and follow
`next`.*

## The inside door: `GetObject(, "Xlide.Api")`

The same routes answer IN PROCESS, with no HTTP and no setting. A live session registers the
door in the Running Object Table, so VBA in any workbook - and any automation client - can:

```vb
GetObject(, "Xlide.Api").Guide                              ' the agent reply
GetObject(, "Xlide.Api").Request("state")                   ' any route
GetObject(, "Xlide.Api").Request("module?name=Mod1", src)   ' second argument is the POST body
```

Measured 2026-08-18, `Request("agent")` per call: **0.10ms from VBA in-process**, 0.36ms from
another process over COM, 0.89ms over HTTP. The call arrives on the host thread and the route
runs right there - the crossing every HTTP request pays does not exist.

Three things to know, all reported by the door itself:

- **Page routes refuse fast** (`ui`, `act`, `eval`, `await`, `console`, `inspect`, `capture`,
  `layout`, `type`, `bench`, `trip`, `reload`, plus `immediate`, `compile` and `session`): a
  page script's answer arrives by the very pump the inside caller is standing on, so the only
  honest inside answer is "use the HTTP door", immediately, with the URL. `agent/routes` marks
  each route's door (`insideDoor: open` or `httpOnly`).
- **No Trust Center setting is required** - the owner's call (2026-08-18): the end-user
  experience must not require flipping "Trust access to the VBA project object model", and
  gating only this door protected nothing while the HTTP door beside it answers the same
  routes for any local caller. The ROT is also WHY this door exists at all: the add-in is a
  VBE add-in, invisible in `Application.COMAddIns` (measured: that collection holds only
  Office add-ins), and the VBE's own `AddIns` collection sits behind the very trust switch
  the door must not require.
- **Several sessions, one name** (the owner, 2026-08-19: the single binding must not be a
  limitation): `GetObject` binds ONE registration however many hosts are live - the first,
  then the survivor when it closes - so whoever holds the name FEDERATES. A target opening
  `@` addresses any live session: `Request("@word/state")` answers for the Word session,
  `Request("@12345/agent")` for a pid exactly, proxied to that session's own HTTP door with
  its own key (never shown to the caller). `Request("sessions")` lists the fleet - pid, host,
  and which session is answering; a host name matching two sessions refuses and says to
  address by pid. A proxied route also escapes this door's host-thread limits: the peer
  answers on its own pool, so even `httpOnly` routes answer through `@`. `Request("agent")`
  still carries `pid` for callers that need to check who holds the name.
- **Retry once on an RPC failure** (`0x800706BE`). A session that is KILLED rather than closed
  leaves its registration behind for a moment, and the next bind reaches that dead holder -
  measured twice on 2026-08-29 with `Test-InsideDoorFleet.ps1`, two Excel processes and the
  name-holder force-killed: the first bind fails with RPC, and the surviving session answers
  by +2s. There is nothing for this product to sweep, because a dead process's ROT entry is
  Windows' to clear and it clears it; a caller only has to ask twice. A killed SOLO session
  leaves no corpse at all - the name refuses with `MK_E_UNAVAILABLE`, exactly as an
  unregistered one does.

## Routes

Read routes are GET, acting routes are POST. Everything answers JSON except `capture`.
Routes that touch the session cross to the host thread with a three second deadline and
answer `{"error":"the host thread did not answer in time"}` rather than hanging - or, once the
heartbeat has been quiet past two seconds, the longer spelling: "the host thread has not
answered for Nms. That usually means VBA is running your code...". A caller tolerating the busy
state matches BOTH, through the client's `hostBusy(error)`; matching one spelling is how
write-rollback aborted mid-poll on a healthy product (2026-08-28).

**Boolean arguments read the same way everywhere.** `1`, `true`, `yes` and `on` are true;
`0`, `false`, `no` and `off` are false; anything else, and anything absent, is the route's own
default. There were two rules until 2026-08-11 and they disagreed about the same text: six sites
took anything that was not the literal `0` as true, so **`perf?reset=false` cleared the counters**
and `native?text=no` asked for the text, while two other sites accepted only an affirmative word,
so `module?live=1` worked and `module?live=yes` did not. Both readings are defensible and no
caller can hold both at once.

| Route | Method | Arguments | Answers |
| --- | --- | --- | --- |
| `agent` | GET | | the front door for a caller that has never seen this api: what the product is, which Office HOST answered (excel/word/powerpoint - the add-in loads in all of them, and an agent about to write VBA needs to know which object model it is writing against), the pid, whether the engine is up, how to call, the in-process alternative, and `next` URLs onward. `agent/routes` is the whole route table as data - each route's arguments, an example, whether a bare GET is safe to try, and how the in-process door treats it; `agent/route?name=x` details one; `agent/examples` is ordered request lines for the common jobs. The discovery file's `agent` field points here, so an agent handed that file is one GET from all of it. Everything under `agent` answers on the pool thread, deliberately: orientation must answer while the host thread is busy or wedged, which is exactly when a caller is most likely to ask what it is looking at |
| `model` | GET | `type` | what the language service KNOWS about this host's object model, passed through from the engine verbatim: bare, the type inventory with member counts (229 types in Excel, 364 in Word - the analyzer's host registry answers per host), the host-injected globals, alias and constant counts; with `type=` (as code writes it, qualified, through the aliases, or by bare display name), that type's members with kinds, returns, signatures and one-line docs. In a host the registry does not know (Outlook, say) the answer is `known:false` with a note - the same honesty the features practice, silence over another host's members |
| `analyzer` | GET | | the analyzer's whole rule catalogue, from the engine: every diagnostic's code, title, default severity, category, evidence kind, confidence, whether it mirrors a VBE compile failure, and the MS-VBAL section it enforces where one applies. What the service will flag, answerable before anything is flagged |
| `state` | GET | | shown module and project, debug mode, unwritten edits, engine up, frame and document-area handles and rects, **the frame's caption as the title bar actually reads it**, **whether the frame is on screen** (`frameVisible`, read off the window; it is where `frame?action=close` 's posted outcome is observed), palette open and visible, surface ready, DevTools port. `frameCaption` is read off the window, not out of the session's record of what it last wrote there, and that is the only reason it is worth having: the host rewrites that window underneath the add-in. Pressing Reset a second time does it, and the caption sat saying "Microsoft Visual Basic for Applications" with nothing able to observe it (2026-08-09). `engineUp` means the engine is ANSWERING, not that the session got as far as constructing the service that talks to it. It was the second thing until 2026-08-08, which made it true from start-up to shutdown whatever the engine did: killing the engine process left this reporting `true` while the editor drew squiggles from the last pass that had run. It is now false BEFORE the engine connects too, which is a real state a launcher should wait through rather than report |
| `windows` | GET | | every editor window: type, caption, visible |
| `native` | GET | `text=1` | the HOST's own editor, underneath the surface that covers it: the ACTIVE CODE PANE's module, project and caret, every pane it holds open, and - in the same reply - what the surface believes it is showing. Run, Step, Compile and ToggleBreakpoint all act on the native pane and its caret rather than on the page, so a disagreement means a Run that executes where the developer is not looking and a breakpoint on the wrong line, with nothing on screen to say so. Carries the pane's CONTENT and the surface's, each reduced the same way - line endings normalised, trailing blanks dropped - so a single changed character registers while the host's CRLF does not. Names agreeing is NOT parity: a surface holding an empty document for a module the host has 42 lines of passes every name comparison and shows a blank editor. `text=1` carries both texts for the run that fails. EVERY open pane carries the workbook's content and the surface's side by side, not only the active one: a background tab holds a copy the developer is not looking at, so a module written from outside while its tab sits behind another goes stale with nothing to notice until it is clicked. `parityAll()` on the client reports which |
| `engine` | GET | `module`, `text=1` | what the ENGINE is holding for a module, against what the surface holds: each text's line count, each reduced to a comparable form, and both texts themselves with `text=1`. There are THREE copies of a module in play, not two. The engine keeps its own live text per module, fed by the page's edits, and prefers it over the copy the project was seeded with whenever a request carries no source of its own. Every finding is measured in whichever it chose, so when a squiggle is drawn away from the word it is about, this is the call that says which side drifted. Nothing could ask it until 2026-08-08, and until then a misplaced finding could only be guessed at |
| `projects` | GET | | EVERY open workbook: name, id, component count, and whether the surface is showing one of its modules. `project` answers about ONE, the one named or the active one, so with two workbooks open there was no way to discover the other's name from the host at all: a probe either knew it in advance or read the page's tree, which is the surface's view rather than the object model's. Two workbooks holding a module of the same name is a designed case here and three separate defects have lived in it, so a suite that cannot name the workbook it means cannot test any of them. The language suite failed exactly there. Cheap on purpose: names and counts, not contents |
| `stats` | GET | | uptime, managed and working memory, handle count, GC counts, placement pass counts and timings, host-thread marshal count and timings, log lines, poll interval, message totals, the COM wrapper counters - and the marshal lane's own eyes: `laneHolder` is the route whose work is ON the host thread right now, null when the lane is free, and `laneHeldMs` how long it has held. Served WITHOUT the host thread, which is the point: the lane is a single file of work, one item that will not finish starves every request behind it while the heartbeat ticks on, and a door dark for four minutes used to blame "VBA running your code" with nothing anywhere naming the real holder (#12, chaos seed 2009959200 - the holder was a reload's teardown). A hold past five seconds also writes itself into the log, so a post-mortem needs no live observer. Under it sits the QUEUE: `marshalQueueDepth` (actions waiting to reach the host thread), `marshalLastDrainMs` (age since the drain last ran), `marshalLastEnqueueMs` (age since one was queued). laneHolder says what is EXECUTING; these say what is WAITING and whether the drain runs at all. In seed 2009959200's jam all read idle - holder null, depth 0, both ages climbing in lockstep for 100+s - which exonerates the marshal machinery and localizes the freeze upstream in the door's request path. `comWrappersGivenBack` and `comWrappersDisposed` are counted SEPARATELY and must always be equal: the first is how many wrappers were handed back, the second how many were actually released. They diverged for the whole life of the product because the release was `(wrapper as IDisposable)?.Dispose()` and the wrapper is a `ComObject`, which does not implement `IDisposable` - so the cast failed silently and every wrapper went to the finalizer thread, which ends the host. `disposed: 0` against `givenBack: 1882` is what finally named it. See [com-wrapper-release.md](com-wrapper-release.md) |
| `log` | GET | `since`, `match`, `max`, `waitMs` | a slice of the shim log and the next byte offset. With `waitMs` it BLOCKS until a matching line appears, so an event can be awaited rather than slept for |
| `doctor` | GET | | the start-of-session checklist: shim and bundle build times, the page's own build stamp, engine and ghost readers attached, dialogs standing. **When the page never reported a stamp it names the CAUSE**, not the silence: boot.js installs ahead of the bundle and pushes any load-time throw to the host, so the finding reads "it THREW while loading: ReferenceError ... at editor.js:1:7" rather than "it may not have finished booting". It distinguishes a page that recorded nothing (still starting) from one that could not be asked (not responding), because those call for opposite next steps. `findings` is empty when nothing is wrong. An engine that is present but NOT ANSWERING is its own finding, and the dangerous one: the engine is started once and never restarted, so the findings on screen are from the last pass that ran and will never change again. That state was called healthy until 2026-08-08 |
| `sessions` | GET | | every live session on this machine: pid, host (excel/word/powerpoint/access), when it started, and which row is the one answering. The fleet the inside door's `@` prefix addresses. Ports, tokens and agent urls are deliberately absent - one door's caller is not handed the keys to every other door; address a peer with `@pid` or `@host` through the inside door, or read its own discovery file |
| `tests` | GET | `action`, `module`, `test`, `file`, `tags`, `outcomes` | the VBA test runner, mirroring the Tests pane exactly. No action lists: every FILE worth showing with its own support state (`missing`/`outdated`/`installed`) and test count, whether a run is in flight, and every discovered test with its latest outcome and the file it came from. Worth showing means it holds tests or carries XlideAssert - plus the file the developer is in, which is listed with nothing in it so the pane's install chip has somewhere to land before the first test exists. Every open file is still ADDRESSABLE whether or not it is listed, so `run&file=` on one with no tests answers `no tests to run in <file>` rather than refusing to find it. **Every open file, not just the front one**: a session holds as many VBA projects as the host has files open, so `file=` narrows any verb to one of them and a module name - which is only unique inside its own file - is ambiguous without it. XlideAssert is a module IN a file, so support is per file: a run refuses for the file that lacks it and proceeds for the file that has it, `action=install` with `file=` installs into one and without it into every file that holds tests and needs it. `action=run` runs everything everywhere, `run&file=` one file, `run&module=` that module in every file holding it, `run&test=Module.Proc` one test; `action=runFailed` reruns whatever last failed, and `runFailed&module=`/`&file=` narrows that - which is what the pane's own Failed button sends while its scope selector is holding one, so a rerun can never run failures the developer has scoped out of sight; `action=debug&test=` runs one test with no trap, so a breakpoint or an error stops in the debugger. **`tags=` and `outcomes=` narrow any run verb the way the pane's filters narrow its list**, and they compose with `module=` and `file=`: `tags=smoke,fast` means tests wearing ANY of those tags (the word `untagged` admits tests carrying none - a project that really tags tests "untagged" gets both meanings, documented rather than disambiguated), and `outcomes=` takes the pane's five chip groups - `passed`, `failed` (covering failed, error and xpass), `xfail`, `skipped` (covering skipped and skip-marked), `notRun` - judged against each test's current standing in its own file, so `run&tags=smoke&outcomes=failed` is the pane's **Run Displayed** button over a smoke filter and a failed-only chip. A selection the facets empty answers in words naming them (`no tests to run in Book1.xlsm with tag smoke`), and a stray outcome word is refused as a typo rather than run as nothing. The facets select what RUNS; the bare list always answers every discovered test, rows carrying their `tags` for a caller to fold its own way. Tests are zero-argument **public** Subs in standard modules under a `' @xlide-test` comment directive (`-skip`/`-xfail` variants; `tags=`, `owner=`, `req=`, `timeout=`, `expected-error=` metadata) - the same authoring surface as xlide_vscode, so a project's tests move between the products unchanged. `run` answers when the whole run has finished, one JSON row per test with status `passed`/`failed`/`error`/`skipped`/`xfail`/`xpass`, the latched failure message, `Assert.WriteLine` output and per-test timing; the pane streams the same rows live. `ranAt` carries when the last run FINISHED (ISO 8601 with offset, null before any run this session), which is what the pane says beside its tally - "7 passed" with no clock cannot tell a result that just landed from one left over from an hour ago. Timeout metadata is carried, not enforced: in-process VBA cannot be preempted, and Ctrl+Break stays the escape |
| `perf` | GET | `reset=1` | recent raw placement and marshal durations, **and the analyzer's cost per method**: calls, time spent QUEUED behind another call, time spent ON the pipe, median, p95, and the individual calls over 120ms. The engine serves one request at a time, so a diagnostics pass over a large module delays every keystroke's completion behind it and a combined figure blames completions. `reset=1` forgets EVERY figure this reply carries - the engine costs, and the placement, marshal, host-read and publish timings beside them - so an experiment measures what it provokes. It used to forget only the engine half, which left the rest describing a different stretch of time with nothing to say so. `hostReadMs` (plus `hostReadCharsLast`, `hostReadFullTransfersLast`, `hostReadSkippedTotal`) times the host-thread COM reads of module source - the audit's C7/C8. `publishUs` (with `publishCount`) times each PublishModules pass in MICROSECONDS, because the unchanged per-tick pass sits under a millisecond and the ring drops zeros - the audit's B23, whose cost was asserted as "a read" until this measured it |
| `journal` | GET | `lines` | one capture of a whole moment: state, dialogs, counters, recent log, recent page traffic |
| `drainfinalizers` | GET | | Forces a collection and WAITS for the finalizers, so a leaked COM wrapper fails NOW instead of minutes later in a stack that names nothing about what created it. A wrapper nothing disposed is released by the finalizer thread, and for the editor's objects that is an access violation the runtime cannot throw: it FailFasts and ends Excel. Run an operation, call this, and if it does not answer, THAT operation left a wrapper behind. It is a BISECTING tool, not a health check: the last route that tried to be both reported a clean bill of health with 8,734 wrappers pending, because it measured the heap rather than the outcome. The count still comes from `stats.comWrappersLive` |
| `history` | GET | | the last 300 STATE-CHANGING requests this door has served (the polled routes - history, log, journal, state, dialogs - are left out, or they would be the whole transcript), a script that replays them, and **`routeCosts`**: each route's count, total and worst time, slowest total first. `routeCosts` counts every route including the polled ones, because the door is the instrument every other measurement is taken through and a route that has quietly become slow makes everything look slow |
| `assert` | POST | `that`, `value`, `timeoutMs` | states an expectation and waits for it, answering with what was actually seen. A missing or empty `that` is refused ON THE POOL THREAD with the claim vocabulary: this route exists to answer while a modal owns the host thread, and its argument guard used to drop exactly those calls into the host marshal |
| `messages` | GET | `last` | recent page traffic both directions, per surface |
| `problems` | GET | `module` | the analyzer's current findings. **Not the same thing as the squiggles.** These are the analyzer's findings for the project as last analysed; the underline a developer sees also comes from the LIVE analysis of unwritten text, which this does not carry. Measured 2026-08-08: an undeclared variable showed an error underline on screen while this answered an empty list. To check a squiggle, ask `ui` with `word` or `line`+`column` and read `at.squiggles`, which is monaco's markers - exactly what draws the underline |
| `locals` | GET | | the Locals panel's context and rows |
| `watches` | GET | | whether stopped, and the Watch panel's rows. READ ONLY BY DESIGN: a watch enters only through the editor's Add Watch dialog, and the VBE object model exposes no Watches collection to add one from inside, so the only way to populate the panel is to type into that native modal. That is synthetic keyboard input, which this product does not do from a route - it tests the synthesizer rather than the feature, and has twice reported a working feature broken (lessons 43, and the search widget). So the `watchHas` assert has no api driver on purpose; `Test-WatchPanel.ps1` is its designated exception, out of gate, driving the dialog with WM_CHAR and needing the VBA project trust setting. If the object model ever grows a watch primitive, this becomes an ordinary route |
| `module` | GET | `name`, `project`, `live=1` | a module's text. Through the session's reader by default; with `live=1` the SURFACE's copy, which is what the developer has typed but not yet written back. That window is where every typing behaviour lives - smart Enter, comment continuation, auto-indent - so without it those could only be checked by eye |
| `capture` | GET | `window=frame\|palette\|form`, `caption`, `selector`, `pad` | a BMP of the window, through PrintWindow. With `selector` it is cropped to that element. **`window=form` photographs a RUNNING form**, matched on any part of its `caption` the way the userform close is, and it is the only picture of a designer's work anything can get: MSForms draws its controls windowless, so there are no child handles to enumerate, and the designer's own collection describes the STORED form rather than the one the runtime painted. Refused by name when no form is standing, rather than quietly returning the frame. `tools\harness\Get-Shot.ps1 -Window form -Caption 'Quarter Entry'` converts it to a PNG |
| `module` | POST | `name`, `project`, body = text | writes the module through the session's writer, with the baseline and engine corrections a host rewrite carries. Answers `error` when the write did not happen: a module that is not there, or text the editor refuses. It used to answer ok to both |
| `dialogs` | GET | | native dialogs standing now, with their buttons, how long the host thread has been quiet, and **`recentlyCleared`: what the guard took off the screen just before you asked**. That last field is there because this route observes something another part of this door actively removes, and an instrument that changes what it measures has to say so in its own answer: an empty `dialogs` means either nothing opened or something opened and was cancelled a second ago, and reading the second as the first has already produced a wrong conclusion here (2026-08-09, twice in one hour). Needs no host thread, so it answers while the editor is stuck |
| `ui` | GET | `waitMs`, `line`+`column`, `word` | the surface as the PAGE describes it: tabs with the labels the strip drew and the MRU order a close falls through, the tree's expansion and unfolded module, panes, page-side dialogs, what text has not arrived yet, the caret, **the main-thread stalls over 50ms** (which is what jank is, and which no host counter can see), and the model/document census a leak shows up in. `dialogs` is asked of the DOM by `aria-modal`, not read off a list, so a dialog added later is reported without anything remembering to add it. Reported from the fields the render reads, so it cannot mistake a stale render for the state, the find/replace widget's query, scope and match count, and the bookmark lines of the model on screen (read from its live decorations, which ride the text as it is edited, rather than from where the marks were put). With `line`+`column` or `word` it also answers `at`: the word there, the COLOUR actually painted on it (read off the rendered span, not derived from a token type and a theme map), its weight/style, and every marker covering it - which is the squiggle. **A colour only exists for a line monaco has RENDERED**, and how many that is depends on the editor's height, which depends on which docks are open - so `at.rendered` says whether the line was on screen at all, and without it a null colour means one of two opposite things. It read null for everything below line 10 of a twenty-line module with every pane open, which is how #17 came to be filed against the semantic pass and xlide_vscode#54 against the analyzer, both of which were working. Reveal the line first (`act do=reveal`), or use the client's `revealing({line, column})`, which is `at` with the scroll done for it. **`statusNotice`** is what the status line is saying, empty when it is saying nothing: the status bar was in no snapshot at all until 2026-08-11, so the explanation a declined action gives could be driven and never observed. It carries both kinds - the ordinary notice that clears itself after five seconds, and a HELD one that stays until the condition ends, of which the only one today is the engine's cold start. `statusPosition` and `statusModule` complete the bar (2026-08-12): the caret readout ("Ln 4, Col 7") and the module name, read off the elements the render writes - the readout is the developer's own answer to where a Run would land, so debugger-features holds it to the native caret while stopped. `sync` is the sync dialog while one is open, null otherwise: direction, folder, mode, busy, the status sentence, and every row with its tick, statuses in the plan's own vocabulary; `act("syncDialog", ...)` is the drive side, and module-sync.mjs uses the pair where it used to querySelector the dialog's private DOM |
| `act` | POST | `do=closeActive\|answerCloseConfirm\|search\|bookmark\|format\|dock\|backspace\|hover\|completions\|signature\|quickFixes\|definition\|references\|rename\|select\|extractMethod\|extractVariable\|inlineVariable\|moveToModule\|extractDialog\|implementInterface\|encapsulateField\|activate\|cycleTab\|split\|expandWorkbook\|unfoldModule\|treeMenu\|treeAdd\|menuBar\|chooseMenuItem\|answerRemoveConfirm\|renameModule\|settings\|sponsors\|closeDialogs\|syncDialog\|key\|focusEditor\|reveal\|editorAction\|editProperty\|toolbar\|designerApply\|designerMarkup\|designerSetMarkup\|designerLint\|designerComplete\|designerHover\|designerHint\|colourPicker\|designerCanvas\|designerSelect\|designerCaret\|designerEventStub\|designerDrag\|designerResize\|designerDelete\|designerToolbox\|designerOpenTab\|designerTabMenu\|designerMarquee\|designerArrange\|designerZOrder\|designerTabOrder\|designerZoom\|changesPane`, plus that action's arguments | drives the surface through the methods a click reaches. Answers `{did, detail}`; `did: false` means the page declined or the action did not land, which is an answer and not an error. **`closeActive` reports the OUTCOME, not the request**: closing a module with unsaved changes raises a Save / Don't Save / Cancel box and the tab stays until it is answered, so `did` is false and `detail` names what is standing. `answerCloseConfirm` with `answer=save\|discard\|cancel` answers it \| `activate` picks a tab by name the way a click picks one: resolved case-insensitively against the OPEN tabs, `project` optional - a name two workbooks hold falls to the shown project's tab and is otherwise refused asking for `project`, and success compares the whole identity so a collision cannot report the wrong workbook's tab as landed. Until 2026-08-13 it passed the bare name to the exact-identity lookup underneath, so `activate` without `project` answered `did: false` about a tab that was on screen \| `search` takes `query` (plus `scope`, `matchCase`, `wholeWord`, `replacement`, `run`) or `close=1`, and TYPES the query so the widget's own input handler runs - assigning an input's value raises no event and the widget would never search. **TYPING IS NOT SEARCHING outside module scope**, and `run` is the press that follows it: `findAll`, `next`, `previous`, `replace`, `replaceAll`, each going through the method that button's own click listener calls. The `input` event only searches when the scope is `module`, so without `run` a project search types a query and searches nothing - and it answered `did: true` while doing so until 2026-08-11, agreeing with a `matches` field that is structurally 0 for every non-module scope. **Read `scopedMatches`, not `matches`, for any scope but module**; `matches` and `current` describe the module-scope engine only. `replaceAll` rewrites text across every module of the project and had no driver of any kind before this. `bookmark` takes `which=toggle\|next\|previous\|clear` - **not `do`**, which is the action selector itself; an argument of that name overwrites it and the door answers "unknown action". `format` runs Format Module, or Format Selection with `selection=1`, and WAITS for it. `dock` takes `pane` and `side=left\|right\|top\|bottom` and moves a pane through THE METHOD A REAL DROP CALLS. The docking gestures had no coverage of any kind before it: `layout` could report the arrangement and `layout?reset=1` could restore it, and nothing in between could move anything, so the whole surface was drivable only by hand. Synthesising the drag was never the answer, because the drop arms on pointerdown and completes on pointerup against a compass that follows the pointer, so a synthetic sequence tests the synthesiser. Answers `did: false` and names the pane when there is no such pane, rather than pretending. `backspace` presses the key `times` over, through `deleteLeft`, which is what the key is bound to: the `type` route cannot express one, because it drives `trigger("keyboard", "type")` and that only ever inserts. It is worth being able to send because in a line's LEADING whitespace Backspace does not delete one character: with `useTabStops` on it takes back a whole indent level, which is the behaviour that makes indenting with spaces bearable now that VBA's refusal to store a tab has removed the choice. Answers the caret and the line's text in `data`. `hover`, `completions`, `signature` and `quickFixes` take `line`+`column` or `word` and answer in `data` by calling the PROVIDER OBJECTS monaco calls, with the arguments monaco passes - so they answer what the developer's editor would, including each provider's refusal to answer for anything but the host-active module. Not the bridge request underneath (that skips the refusal and reports healthy while the screen is dead), and not by scraping a widget (monaco reuses an open one, so a scrape returns the previous answer in every state). `definition` answers where F12 would go WITHOUT moving the caret, which matters because moving it is what cancels a symbol-navigation request. **`references` answers the Find All References list in `data`**, each row carrying `kind` - read, write or readwrite, the analyzer's own classification (xlide_vscode#55) that Extract Method's signature derivation rests on, and with `open=1` LEAVES THE DIALOG STANDING, which is what Shift+F12 does: the dialog's rendering is the feature, because it draws a module with no tab open where monaco's own peek cannot, so the data-only default returns rows and exercises everything except the thing the feature exists for. Both forms share one lookup, so the dialog cannot list a set the data form would not, and `ui.dialogs` sees the open one. `syncDialog` drives the OPEN sync dialog through its own controls - `press` a named button, `tick` a row by file with `on`, `folder` to type the path - with `ui.sync` as the read side; before it, the dialog's rows could only be reached by querySelector from a harness file, a test of the selector as much as of the dialog. **`rename` CHANGES STATE**: it is the call F2 makes, so the host applies the edits across every module using the symbol and the surface republishes - undo with `undoRename`. It answers the provider's own refusal, word for word as the rename box shows it. **`extractMethod` CHANGES STATE TOO**: `startLine`, `endLine` and `name` select those lines, run the same editor action the right-click menu and the lightbulb run, type the name into the dialog that opens and press Extract - so a check that passes has driven the menu path and not a back door. The lines default to whatever is selected. A refusal comes back in the dialog's own words and the dialog is cancelled; `ui.extract` is the read side while it stands. `extractDialog` drives that dialog alone with `type=<name>` or `press=extract\|cancel`. `select` takes `startLine` and `endLine` and selects those whole lines, or `clear=1` to put the caret back with nothing selected - the door could not make a selection at all before Extract Method needed one, and `caret` selects nothing. **`implementInterface` CHANGES STATE**: it writes the stubs the module on screen owes the interfaces it declares, `interfaceName` narrowing it to one of them, and answers `{interfaces, added, refused}`. There is no dialog because there is nothing to name - the members, their names and their signatures are the interface's - so a refusal comes back in `detail` rather than staying on screen. **`encapsulateField` CHANGES STATE**: `fieldName` names a module variable of the module on screen, which becomes `Private m_<name>` behind a `Property Get` and a `Property Let` - or a `Property Set`, for a type VBA assigns with one - and answers `{field, backingField, accessors, refused}`. Nothing outside the module is rewritten, because the property keeps the variable's name. **`extractVariable` CHANGES STATE**: `startLine`, `startColumn`, `endLine`, `endColumn` and `name` select that range - columns, because an expression is part of a line - run the same editor action the menu runs, and name the expression through the same dialog Extract Method uses. The declared type and whether the assignment needs `Set` are the ANALYZER's answers (`resolveExpressionType`, xlide_vscode#61), not this product's. **`inlineVariable` CHANGES STATE**: `word` or `line`+`column` names a local, whose declaration and assignment go and whose every use takes the value; answers `{variable, value, replaced, refused}`. It inlines only an ATOMIC value - a literal or a name with its member chain - because in VBA brackets around an argument pass it by value instead of by reference, so an inliner that parenthesises for precedence changes how every call site binds. **`moveToModule` CHANGES STATE ACROSS MODULES**: `targetModule` and a position inside the procedure move it into that module and repoint every QUALIFIED call site at the new name; answers `{moved, from, to, modules, requalified, refused}`. An unqualified call is left alone, because VBA resolves it across the project. It refuses a procedure that uses anything Private to the module it would leave, naming what would be stranded. `treeMenu` takes `module` or `workbook` and RIGHT-CLICKS that row, answering the menu it produced as a ` \| ` separated list, which is the only way to those menus: they hang off a DOM listener with no method behind them, and what a menu offers is a claim about the class of the thing clicked, so reading it is the test. A collapsed workbook has no component rows, so `expandWorkbook` comes first. `chooseMenuItem` takes `label` and fires `pointerup`, which is what the menu listens for; a trailing ellipsis need not be spelled. **`answerRemoveConfirm` is the only thing that removes a component through the product path**, with `answer=remove\|cancel`, and cancel is what anything unrecognised gets. `treeAdd` takes `workbook` and presses that row's plus; the plus is only VISIBLE on hover and no script can provoke :hover, so this presses the control rather than the pixel and leaves whether it appears to a probe that can move a pointer. `menuBar` opens the surface's own menu, the wrench, and is the one menu whose items come from the HOST - so it waits for them and says so when they never arrive, because an empty menu and a menu that never came are different failures. `chooseMenuItem` drives all three: the menu bar and the context menus both activate on `pointerup`. **`renameModule` is the product's rename**, taking `module`, `newName` and an optional `workbook`: the one the tree's Rename item runs, which renames the component AND every mention of it across the project, and answers how many mentions in how many modules. It is not `component?action=rename`, which is the fixture primitive and leaves every reference pointing at a name nothing answers to. Nothing could drive this until 2026-08-10, because the only route to it was a person typing into `window.prompt`. **`editorAction` runs any registered editor action by `id`**, awaited when it answers a promise. Several of this product's features are only an `editor.addAction` reachable from a context menu or a key, and until this existed the door covered them by calling the shim underneath, which proves the operation works and not that the menu item does: `editorAction id=xlide.undoRename` is the Undo Rename item itself, where `undoRename` is the shim's own undo. Redo is registered as a COMMAND rather than an action, so `getAction` finds nothing for it; **undo is this surface's own action, `xlide.undo`**, because it decides between monaco's undo and the host's whole-project reversal of a rename - triggering monaco's built-in undo directly drives a path Ctrl+Z no longer takes. Use the `undo` action for either. **`editProperty` takes `name` and `value` and CHANGES REAL COMPONENT STATE** through the object model's own property bag - `(Name)` renames the component, and there is no undo behind it. It edits whichever component the panel is showing, so open or select one first and read `ui.properties` to see which. **A row is only there if it is a VALUE the developer can see or set.** An object-valued property is not one, with two exceptions: `Font` shows as its parts (`Font.Name`, `Font.Size`, `Font.Bold`, `Font.Italic`, the same rows a control's selection draws), and a PICTURE (`Picture`, `MouseIcon`) shows as itself. `Controls`, `ActiveControl` and `Selected` are not listed - runtime state the native panel does not show either. A `_`-prefixed name and anything the type library marks hidden or restricted go too, which is how `_Font_Reserved` and `DesignMode` left. **A PICTURE ROW carries `picture: true`, says what it HOLDS rather than a value (`(None)`, `(Bitmap)`, `(Icon)`, `(Metafile)` - the native designer's own words, because MSForms keeps the pixels and forgets the file they came from), and carries the picture itself as `previewBytes`** - the thumbnail's size, not the thumbnail: the bytes are a whole bitmap in base64 and a snapshot is read on every wait. Write one with `editProperty` and a FILE PATH; write `""` to clear it, which is the row's own Clear button. The panel's Browse raises the machine's file dialog to produce that path and then takes exactly this road. **`writable` comes from the library's own PUT**, not from the variant that came back - and **a row with no setter is now LEFT OUT** (the owner, 2026-08-16: "if theyre jot settable; dint suow them"), which on a UserForm removes `CanPaste`, `CanUndo`, `CanRedo`, `InsideWidth` and `InsideHeight`: the first three are questions about the editing session rather than about the form, the last two are measurements. `(Name)` stays, because it is the VBE's rename rather than a designer property. **`ui.properties.component` is `""` when the panel is showing nothing at all**, rows and header both, which is where it goes when the component it was aimed at is removed and no other is open: until 2026-08-15 it kept the name and every row of a component the tree no longer listed. **Values are spelled the way the developer writes them**: where the type library names a property's values, the row shows the member (`fmCycleAllForms`, not `0`) and carries them in `options`. A COLOUR reads `#rrggbb` (the owner, 2026-08-16), except a SYSTEM colour, which is not a colour but a question - what does this machine call a button face - and reads by name (`Button Face`, not `&H8000000F&` and not the `#f0f0f0` it happens to resolve to today, which would freeze the answer into the form). The row's `swatch` carries what it resolves to now, host-resolved through GetSysColor, and the write takes any of the four: `#rrggbb`, a system colour's name, `&Hbbggrr&`, or the raw number. **A FONT row is a picker too** (the owner, 2026-08-16): `Font.Name` carries this machine's own faces in `options`, enumerated through GDI's `EnumFontFamiliesEx` once per session and sorted, with the `@`-prefixed vertical variants left out the way every native font list leaves them; `Font.Size` carries the usual ramp, which is the one list here that is written down rather than measured, because a point size is a number and there is nothing to ask the machine. Both stay TYPEABLE, like every other options row: MSForms stores a face as a plain string, and a form written on another machine may name a font this one has never had. A DOCUMENT component's library is EXCEL's rather than the editor's, reached through the host object itself, so a worksheet reads `xlSheetVisible` and `xlNoRestrictions` where it used to read `-1` and `0`. The write takes either - a member name, a hex literal, or the raw number - and answers with what the row HOLDS afterwards, which is why a written `0` answers `fmCycleAllForms`. A REFUSAL is the host saying so on the status line, not the row failing to echo the request: comparing the two was right only while every value had one spelling. It answers the HOST's outcome, not the post: the panel's controls set the row as they send, so the value reading back proves only that it was asked for, and the act waits for the host to republish before comparing. A refused write answers `did: false` with what the row actually holds and whatever the status line said about it. Together with `ui.properties` this closes the last user-visible surface that had no api presence in either direction. ****`reveal` scrolls a line into view**, taking `line`, which is what a developer does before answering what colour a word is: monaco builds spans only for the lines in the viewport, so `ui?line=` can report a colour for nothing else, and how tall the editor is depends on which docks are open. It renders synchronously on the way out, so the read after it sees the revealed line rather than the frame before it - the alternative to a sleep, not a companion to one - and answers the visible range in `data`, which is the observation that proves it worked. A line outside the model is refused rather than clamped. `toolbar` presses a strip command by `command` id** - `openSync`, `objectBrowser`, `openPanes`, `openHelp`, the indent and fold cluster, about thirty of them - through the button's own click, which is why a disabled one is refused as disabled and says whether it needs break mode. With no argument it answers the strip's contents in `data.commands` as `{id, label, disabled}`. This is the driver half of the sync dialog, the Object Browser and the Panes menu: `press` is a keyboard key and the `command` route below is the NATIVE editor's command by name, so until 2026-08-11 the page's own command surface could only be reached by finding a button by its `data-command` attribute and clicking it, which three harness files did and which keeps passing after the button is renamed, disabled, or left out of the build because its editor action was not bundled. **`settings` with `key` and `value` changes a setting THROUGH THE PAGE**, which is a different path from the `settings` route below and the only one that can carry this class of defect: the route replaces one field of the stored settings on the host side, the page posts the whole object as it believes it to be. Its post was one field short of the six for months, so every change through the dialog reset the developer's sync planner. Bare, `settings` still just opens the dialog. **`designerApply` takes `module` (plus `project`) and `markup` and applies the document THROUGH THE DESIGNER TAB'S OWN PATH** - the view's document and the host round trip a Ctrl+S takes - so a suite proves the tab works, not merely the `designer?action=applyMarkup` route the tab shares its service with. Answers the outcome in `data` (`ok`, `added`, `removed`, `set`, `refused`) with the refusal's own wording in `detail`; the tab must be OPEN (`pane?action=open&face=design` first). An OK apply is followed by the HOST'S OWN File Save - Ctrl+S means "save the workbook" everywhere in the product, and the designer's does not quietly mean less - while a refusal saves nothing. `designerMarkup` answers the tab's document as it stands in `data`, which is also how a suite waits for the projection to arrive before editing it. `designerSetMarkup` sets the document WITHOUT applying - the typing path - and `designerLint` answers the document's current squiggles in `data` as `{line, message, severity}`: Core's tolerant parse, every finding where the strict apply stops at the first. **`colourPicker` drives the panel's own colour picker**, which is xlide's rather than the platform's: `property` names the colour row and presses its swatch, `choose` picks - a `#rrggbb` from the palette or a system colour's NAME - and with neither it answers the open picker's state (`property`, `value`, and how many colours each half offers). A pick commits through the row's own path, so the write is an ordinary `editProperty` underneath and the host's answer is what proves it. The palette is GENERATED from a ramp rather than written down, so a probe should read `.colour-swatch` values and choose one rather than naming a hex that may move. The System half is the host's `setSystemColours`, measured through GetSysColor at load. **`designerComplete`, `designerHover` and `designerHint` are the markup's LANGUAGE SERVICE**, each taking `line` and `column` and answering in `data` what a developer at that spot would see - the real providers, called the way monaco calls them, so there is no second copy of the answer for a probe to read. Each puts the CARET there first, because raising any of these widgets by hand means being there (and a caret inside a control's block selects that control, which is the same link `designerCaret` drives). `designerComplete` answers `{label, detail, documentation, insert}` per suggestion: the control KINDS where a line can open one, filtered by what the container takes (a MultiPage offers only `Page`), each arriving scaffolded as a whole header with its name, caption, position and MSForms' own drop size as tab stops; the owning kind's PROPERTY names where a line can set one; the attribute names the element does not carry yet, and none of them twice on one tag; and on the value's side of a `=`, that property's enum MEMBERS by the name the developer writes, plus `True`/`False` and what an untouched control holds. Nothing is offered inside a caption. Where the MACHINE can be asked rather than the type library, the measured values come too: `FontName` offers this machine's own faces, the same list the panel's Font.Name row shows. Each suggestion answers `replaces` as the columns accepting it would rewrite, which is half of what a completion is - a face offered where a string is already open takes the quotes with it, so `FontName = "Tah` completes to `FontName = "Tahoma"` rather than nesting a second string inside the first. `designerHover` answers what the LINE does not: over a kind, what that class of control IS - and its coclass only where that is not simply `Forms.` plus the kind, since for the standard fifteen the ProgID restates the word under the pointer (the owner, 2026-08-16: "says that on all of them... seems not helpful"); over a control's NAME, the control declared the way VBA declares it (`OkButton As MSForms.CommandButton`) with the class described under it, which is the same sentence hovering that name in the code-behind gets; over a property path, its declared type, its untouched value and its members; over an enum member, the number behind it; over a clause keyword, the unit. What it never does is read the line's own geometry back - the position and the size are already on screen under the pointer. `designerHint` is the header's grammar - the element's attributes - `Name`, `Caption`, the geometry - and the Form's own tag takes no position - answering `{label, active, parameter}` where `parameter` is the clause the hand is on, followed by the clause KEYWORD standing rather than by counting words. All three answer from the `vocabulary` route's table, which the page asks for once per session. `designerCanvas` answers what the canvas is SHOWING in `data`: `draft` says whether the picture is the document's unapplied preview or the applied projection, `controls` carries every rendered control's name and placed position, `selected` names the selection ("" is the form, null none), `markupLine` is the markup caret's line, and **`markupBlock` is the selection's whole block** as `{from, to}` - the header line and everything indented under it, which is what the markup half washes. Read it rather than counting `.designer-block-mark` elements: monaco draws a decoration on the next frame and only for the lines in view, so a DOM count measures its renderer and the scroll position. **`pictures` is every picture the canvas is actually DRAWING**, one row per control (the form's own answers to `""`) with the data URI's `bytes` and `where` it was placed - `contain center center` for a zoomed surface picture, `row center` for one beside a caption. The size rather than the bytes, for the reason the properties panel's `previewBytes` gives; the pixels are proved against a photograph of the running form, which is the only thing that can prove them. **`designerCaret` takes `line` and puts the markup caret there**, which is the drive side of the same link running the other way: a caret inside a control's block selects that control on the canvas, a property line counting as the thing it describes, and the Form line selecting the form. **`designerSelect` is the canvas click**: `control` selects by name, omitted selects the FORM, the native handles dress it, the markup caret lands on its line - and the Properties panel FOLLOWS host-side: a selected control's curated rows (name, caption, geometry, state, colours, font) show there, `editProperty` writes them through the designer service (the same write an api `set` makes, tab re-projection included), and selecting the form's ground returns the panel to the component. **`designerEventStub` is the canvas double-click**: the host writes the control's DEFAULT event handler into the code-behind when it is absent (Change for the value-bearing kinds, Click for the rest and the form) and navigates to it when it stands, never duplicating - `control` omitted means the form's own `UserForm_Click`. **`designerDrag` is the canvas DRAG** (M5): `control`, `dx` and `dy` in POINTS, sent as the real pointer sequence - the press goes to whatever the hit test answers at the control, so an invisible overlay over the canvas fails the act instead of hiding behind it, and the move crosses the drag threshold before the drop. The drop rewrites the control's line in the DOCUMENT as one undoable edit: `designerCanvas` reports `draft: true`, `dirty: true` and the new position (in points, like the document), the form does not move until the tab's Ctrl+S - `command save` - and one undo takes the whole drag back. Arrow keys nudge the selection a point at a time through the same commit; drive them with a `keydown` on `.designer-canvas-scroll`. **A drag that ends over ANOTHER container reparents**: the control is carried into whatever container the pointer is over as the gesture runs - a Frame's client, a MultiPage's open page, the form's own ground - and the drop moves its whole block under that container in the document, re-indented, with its position in the new container's coordinates. What decides is where the POINTER ends, not how far it travelled, so a control near a container's edge reparents on a short drag; a container cannot be carried into itself or anything it holds. Remember that the pointer path has to be ON SCREEN: a drag whose end point is outside the canvas box hits nothing and the control simply clamps at its parent's edge, which is what a small window produces. **A reparented control is a new control** when the save carries it - the apply's diff calls a changed container a remove-and-add - so it keeps what the markup prints and is born again at its defaults for everything else (a measured example: the tab index and the inherited font). **`designerResize` is the canvas RESIZE**: `edge` names the handle (`nw`, `n`, `ne`, `e`, `se`, `s`, `sw`, `w`), `dx` and `dy` pull it in POINTS, and `control` omitted means the FORM's own frame - whose line takes a size and never a position. It selects the thing first, the way a hand must, and presses through the hit test at the handle, so a handle nothing can reach fails the act. West and north edges move the origin as well as the extent; a pull past the opposite edge stops at the floor size (4 points for a control, 24 for a form) rather than inverting the box. Shift+arrow is the keyboard's resize, same commit, same undo. **`designerDelete` is the canvas DELETE**: it selects the named control and presses the real Delete key on the canvas, so the act cannot pass on a path the keyboard does not take. The control's line leaves the DOCUMENT with everything indented under it - its properties, and a container's children - as one undoable edit; the form keeps the control until the tab's Ctrl+S. Selection lands back on the form. `designerCanvas` also answers `undoable`, the document's own "there is a gesture to give back", which is what tells a timed-out wait apart from an empty stack. **`designerToolbox` drags a KIND out of the xlide palette**: `kind` names it, `left` and `top` are POINTS from the form's client origin, and the real pointer sequence runs from the palette button to that spot. The drop resolves the deepest CONTAINER under it - a Frame's client, a MultiPage's page, the form's ground - so a point over a Frame adds the control INSIDE it with that frame's own coordinates; `detail` answers the name the drop gave (the kind plus the first free number). A point off the form adds nothing and answers `did: false`. **`designerOpenTab` opens a tab of a MultiPage or a TabStrip** - `container` names it, `tab` is a page's NAME, the tab's label, or a 1-based position - through a real press on the real tab. A MultiPage draws that page's controls and selects the PAGE, which is the native designer's gesture: the Properties panel follows to the page, the markup caret lands on its line, and a drop then lands on the page being looked at. A TabStrip only marks the tab, because its tabs are an index rather than containers and the runtime draws the same controls under every one. The read side is `designerCanvas`'s `containers`: one row per tabbed container with its `tabs`, the `open` index, and the `page` that index names. **Which page is open is VIEW state, and that is a designated deviation from the native designer**, which writes the container's `Value` and dirties the form: reaching page two must not rewrite the developer's form, so `dirty` stays false across an open, and a form that should OPEN on a page says so through the Properties panel like every other unprinted property. Selecting anything INSIDE a page opens that page - `designerCaret` on a line in its block, `designerSelect` by name - which is what keeps the two halves pointing at one thing. **`designerTabMenu` right-clicks a MultiPage's tab strip** and answers its items as a ` \| ` separated list (`New Page \| Delete Page`, the native pair), marking a disabled one `(disabled)` - which is what Delete Page is when the MultiPage has no pages left. `tab` names a tab to right-click, and opens that page first so the item acts on the page named; omitted, it right-clicks the strip and Delete Page still acts on the page that is open. Choose with `chooseMenuItem`. A TabStrip opens NO menu and answers `did: false`: its tabs are not in the markup dialect, so there is no line to add or remove. Both items write the DOCUMENT as one undoable edit - New Page appends `Page PageN "PageN"` under the container and opens it, Delete Page takes the page's line and its children - and the form has neither until the tab's Ctrl+S. `designerDelete` reaches a page by name for the same commit. **`designerZOrder` is Bring to Front / Send to Back** - `front=1` for the front, `control` for one of them or omitted for the whole selection. It writes the MODEL rather than the document, the one canvas gesture that does: MSForms' Controls collection is not in z-order and does not move when ZOrder is called, so the dialect has nothing to say and `designerMarkup` has nothing to show. The read side is a photograph of the RUNNING form. **`designerTabOrder` drives the Tab Order dialog**: `open=1` shows it for the container the selection sits in (a control inside a Frame opens the FRAME's list, because tab order is per container in MSForms), `control` with `move=up\|down` sends one control a place, `close=1` presses its Close button, and with none of them it answers the order the list is showing. A move is a single TabIndex write through the host's own SetControlProperty and MSForms renumbers the rest, so read the outcome off `designer` rather than off the list. A control with NO tab stop - an Image, whose tabIndex reads null - is not listed at all. **`designerZoom` sets how large the canvas draws the form**: `to` is a percentage or `fit`, and with neither it answers what is showing, in `data` as a number. The picture is a transform on the form, so everything a gesture does stays in POINTS - a drag of twelve points moves twelve points at 200% as at 100%, which is the property this feature is really about. Fit measures the canvas as it stands and answers what it settled on |
| `trip` | GET | `what=pagecall`, `n` | times a round trip ACROSS the boundary, wall clock from asking to observable. `bench` times the page and `perf` reports the host; this is the crossing neither measures. Read `pagecall` alongside the rest, since it is the floor every other figure contains. **A route body holds the host thread, and a message POSTED to the page is delivered by that thread's pump, so nothing whose effect arrives that way can be observed from inside a request at all** - those are measured across requests, in the client (`tripCaret()`) |
| `eval` | POST | `surface`, `waitMs`, body = script | runs script in the live page and returns its result as JSON. A PROMISE is awaited, so `(async () => ...)()` works. Prefer `ui` and `act` where they fit |
| `await` | POST | `surface`, `waitMs`, body = predicate | polls a predicate IN the page until it is true, answering `met` and how long it took. One request instead of a caller's poll loop |
| `layout` | GET | | the whole visible arrangement: dock sections and their groups, editor groups and their tabs, sizes, open documents, whether a drag is live |
| `layout` | POST | `reset=1`, `waitMs` | puts the arrangement back to the default and waits for the page, so a probe that dragged panes about can clean up in one line |
| `inspect` | GET | `selector`, `styles`, `rules=1`, `max` | elements matching a selector: box, classes, hidden, the computed values of the styles asked for, and which CSS rules claim them |
| `bench` | GET | `what=tabswitch\|layout\|type`, `n` | times a scenario in the page and answers min, median, p95, max, and the raw samples |
| `console` | GET | `last` | what the page said to itself: a ring of console lines, installed by **boot.js ahead of the bundle** rather than at page ready. That ordering is the whole value: a bundle that throws while its modules initialise never reaches ready, and the ring used to be created there, so this route answered `{"installed": false, "lines": []}` at exactly the moment somebody was asking why the screen was blank (2026-08-09). The JavaScript context survives a module that throws, so the ring can be read out of a page that never booted. It carries uncaught errors and unhandled rejections as well as console output |
| `reload` | POST | `waitMs` | reloads the page and waits for it to come back, answering with the bundle stamp it is now running and whether that is behind the one on disk |
| `dismiss` | POST | `button`, `caption` | clicks a dialog button by name. Explicit, so it will press OK if asked. A missing or empty `button` is refused ON THE POOL THREAD: dismiss made with a dialog standing is the one call that must never wait on the host thread that dialog owns, and its argument guard used to send exactly that call there |
| `guard` | POST | `on=true\|false`, `forget` | turns the dialog guard on or off and answers what it has cleared. While ON, a NOTICE this door did not raise is cleared too - see below. Off by default |
| `userform` | GET/POST | `action=close`, `caption` | the captions of every RUNNING form (the forms runtime's own `ThunderDFrame` windows), or with `action=close` posts the close its X would, matched on caption - the first one standing when none is named. Off the host thread on purpose, twice over: enumeration is pure Win32, and the one moment this route matters - watching or closing a modally running form - is exactly when the host thread is parked inside the Run command until that form goes away. The partner of `command` `run` with a designer tab active, which is what launches one |
| `compile` | POST | `waitMs` | compiles the project and answers its errors as DATA, clearing the modal it raises. **`compiled` means no dialog appeared within `waitMs` AND the command actually ran** - the editor has no positive report to give, it answers a compile with a modal or with nothing. `started` is the second half and the reason this is two fields: a greyed Compile item ran nothing, so no dialog appeared, so the reply said the project compiled. The debugger suite reads this as its precondition, which made every check behind it a test of the dialog guard |
| `documents` | GET | | the documents the surface holds TEXT for, with line counts, unwritten flags, and which is active. Not the same list as the tabs |
| `changes` | GET | `action=text\|diff\|snapshot\|accept\|restore\|reject`, `project`, `round`, `module`, `which=before\|after`, `label`, `limit`, `by` | the change log: what happened to a project's module code, by whom, in rounds. No action lists the rounds newest first, the one still running at the front, each with who wrote it, what it was called if anyone said, and one entry per module carrying `+added -removed` worked out from the texts the log kept. `text` answers what a module held before or after a round, `diff` lines the two up as the import dialog's own comparison does. `snapshot&label=` ends the round that is running and names it - it costs nothing, because a round is a divider rather than a copy - and `accept` marks every round so far as reviewed, removing none of them. **READ-ONLY: there is no revert here and there is not going to be one.** Putting text back is a write, and this door already has a hardened one at `module`: read the text you want and write it, and the result lands in this log like any other write, as visible and as reversible as what it undid. A round is ended by a snapshot, by the writing hand changing, by a workbook save, or by five minutes' silence. The log is held IN MEMORY for the life of the session and writes no file anywhere: nothing in production may depend on an external log file. Past 64 MB of held text the oldest rounds let their texts go and say so (`held: false`), keeping their entries. What it records is MODULE CODE - written, added, removed and renamed - written through this product: not references, not project properties, not a form's design. It says so in its own `covers` field, and leads with what IS recorded, because a developer's own typing is in there under their own name and that is the part they are most likely to doubt. The reply's `total` is how many rounds the log HOLDS, which is not always how many are in `rounds`: the list stops at `limit` (200 by default), and a view that stopped without saying so made "the newest two hundred" and "all of them" the same answer. `by=` on any route names the caller and is what makes the log worth reading once two agents are working; without it a write through this door is recorded as `unattributed`, because it is certainly not the developer typing. **`restore&round=N` makes what the log remembers true again** - the whole project, or one module with `module=`, back to the state after round N (0 = before everything the log saw), and `reject` restores to the accept mark. A restore LANDS AS A ROUND, attributed to `by`, so it is always itself restorable: aim at the round before it. Its cost is one write per module that actually differs - nothing replays history - measured at 446ms to restore away from a 15,002-line module and the price of the write (2.1s) to restore forward into it. The answer carries `outcomes` per module because a restore can partially decline in words: a stopped debugger refuses everything, a module holding unwritten edits is skipped by name, a removed FORM is not re-added (its design was never recorded), a module whose text the log aged out says so, and a rename since the boundary is carried back name and all. In the pane: a Restore control on every closed round, Restore this module on the full-size comparison, and Reject beside Accept - each confirms first and reports module by module. **The listing carries `sinceAccept`** - files, +added, -removed, and per-module entries, text against text so a write hand-reverted counts as nothing - and `action=diff&since=accept&module=` lines the accept-mark text against the LIVE text. The pane wears it as a hybrid button beside Reject: the counts on its face, the changed modules behind a click, and a module's row opens it in the editor with the changes painted - green lines where text arrived, the deleted lines themselves as red-tinted ghosts with their old line numbers, and both marked in the scrollbar. The paint, the button and the list all follow the log: a write, an accept, a reject or a restore moves a stamp and the pane re-reads a quiet moment later |
| `sync` | GET/POST | `direction=export\|import`, `action=plan\|apply\|settings\|browse`, `folder`, `mode`, `select=checked\|all`, `project`; POST body names the rows, one id per line | import and export, through the SAME call the dialog's button goes through, so a plan read here is the plan drawn there and an apply leaves the project where the button would have left it. Without `action` it answers the plan and changes nothing. `settings` reads what the project remembers, and writes it when a value is named. `browse` raises the system's folder chooser and BLOCKS until somebody answers it, so it is the dialog's, not a harness's |
| `component` | POST | `action=add\|remove\|rename`, `kind=1\|module\|standard`, `2\|class`, `3\|form`, `name`, `newName`, `project` | refuses a name the project already holds IN WORDS, rather than handing back the editor's bare `Unexpected HRESULT` which names neither the problem nor the module; refuses all three while the project is stopped in the debugger, naming the way out. Otherwise it adds, renames or removes a component FROM INSIDE, so a fixture can be built without "Trust access to the VBA project object model". `name` is read back from the component; a refused name adds nothing. A kind that is neither a number nor a word it knows is REFUSED, not defaulted. **`remove` runs the product's own removal**, the one the tree's Remove item runs, so it also drops the page's unwritten edits for that module, the baseline this session last wrote, and its breakpoints - the bare COM call it used to make left all three behind, pointing at a module that no longer existed, and a harness removing a component left a different machine than a developer removing the same one. It skips only the confirmation, which is the page's; a document module is refused here as it is there. **`rename` now adopts the rename the way the Properties grid does**, re-keying the write baseline and the breakpoint record onto the new name: setting the name on the component renames it in the collection and nowhere else, so a breakpoint the developer could see was one this session could no longer find. It is still the fixture primitive and still renames nothing that MENTIONS the module - that is `act renameModule` |
| `defaults` | GET | `type` | what a control of that KIND holds untouched, as `{type, count, properties:[{name, value}]}` - the inventory the markup projection compares against so it can print only what a developer CHANGED. Measured from a bare instance of the coclass MSForms registers (`Forms.CommandButton.1` and the rest, the same ProgIDs `designer?action=add` takes), created in-process and released: **no workbook is opened, no form is created, nothing appears on screen**, and no table written here can rot against the MSForms this machine has. Cached per kind for the session, because a registered coclass does not change under a running Excel. It reports its own gaps rather than papering over them: a control that will not come up outside a container answers few properties or none (a MultiPage answers one), a kind with no ProgID of ours answers zero, and a bare control's FONT is not the font it wears on a form - a control inherits the form's, so fonts are compared against the form and never against this |
| `vocabulary` | GET | `module`, `project` | THE MARKUP LANGUAGE'S WHOLE VOCABULARY, as `{kinds:[{kind, progId, container, properties:[{name, type, default, doc, members, colour}]}]}` - exactly the table the designer tab's completions and hovers answer from, so a suite can compare what the page OFFERS against what the host MEASURED rather than against a list written down twice. Measured the way `defaults` is (a bare instance of each coclass, no workbook, nothing on screen) and read through the same ITypeInfo the Properties panel spells its enums with: a property carries the type it is DECLARED as, its enum's members, the library's own sentence about it, and what an untouched control holds. Two measured facts about that: MSForms answers NO help strings, so `doc` is empty for every standard kind and the field is there for libraries that document themselves; and a control's font rides FLAT (`FontName`, `FontSize`, `FontBold`), which is how the extender carries it, so there are no dotted paths here. `module` names a live form so the FORM's own entry can be described - it is the one kind with no coclass to instantiate bare - and without it every other kind still answers. The page asks once per session and holds the answer, so no keystroke waits on this |
| `designer` | GET+POST | `module`, `project`; POST adds `action=add\|remove\|set\|zorder\|liveness\|baseline` with `type`, `name`, `parent`, `left`/`top`/`width`/`height` for add, `name` for remove, `name` (omitted = the form), `property`, `value`, `as=text\|number\|flag` for set, `name` and `to=front\|back` for zorder, nothing for liveness, and `module` for baseline | a UserForm's design as data, and the three mutations a form fixture is made of - docs/userform-designer.md's M1 instrument. The GET walks the MSForms designer object model and answers the form (caption, size, colours, zoom - Width and Height come from the component's own Properties collection, the native panel's source, because the designer object does not carry them) and every control with its identity, container and geometry: `type` is the friendly toolbox name mapped from the extender's internal interface (`IMdcText` is a TextBox), `parent` names the Frame or Page that holds it, a MultiPage's Pages are rows of their own, and a property a control does not carry is null rather than guessed. Reads are tolerant PER PROPERTY, because controls differ and a missing member is an answer. **`add` goes through `Controls.Add` with a ProgID** - the same call the native toolbox makes, which is what keeps a form built here byte-compatible with one built by hand - takes a toolbox name or any full ProgID, places into a named Frame or Page, and an add that cannot then be placed is taken back out, so it produces the control asked for or nothing. **`remove` resolves the OWNING collection first**, containers before the top level, because Remove belongs to the collection the control lives in. **`set` answers what the property reads back**, the Properties panel's convention; one level of dotting reaches an object-valued member (`Font.Size`), and a form property the designer lacks falls through to the component's Properties collection, so the form's own Width is settable the way the native panel sets it. **`Picture` and `MouseIcon` take a FILE PATH** and answer what the control then holds (`Picture is (Bitmap)`); `value=` empty clears it, which is `Set x.Picture = Nothing`. A file that is not there, or is not a picture this machine can decode, is refused by name and nothing is written. Two loaders, in this order: OLE's own reads BMP, GIF, JPEG, ICO, CUR, WMF and EMF and keeps an icon an icon; GDI+ catches what it refuses, which is PNG and anything else a codec is installed for, flattened to a bitmap with its transparent parts taking the control's own BackColor. The READ side is the `picture` block on the form and on every control that holds one: `kind`, the size in `width`/`height` PIXELS, and the placement (`sizeMode`, `alignment`, `tiling`, `position`). Described rather than carried - a bitmap in base64 is megabytes and this route is read in loops. A control holding nothing answers no `picture` block at all. **`format=markup` answers the same walk as text** in the form markup language (docs/userform-designer.md, the markup layer): identity, containment, geometry and caption, deliberately narrow, so an apply can never erase what the text does not speak of. **`action=applyMarkup` takes the document as the request body** and applies it as a NAME-KEYED DIFF - controls only in the markup are added (a kind outside the toolbox needs a `ProgId` line), controls only in the model are removed, matched controls take their header geometry, caption and property lines, pages are reported rather than created or removed. A document that does not parse applies NOTHING and the refusal carries the line; a refusal partway answers `ok: false` with exactly what landed first. A document that uses one NAME twice applies nothing either, and says so in the product's own words - the diff is keyed by name, so two controls of one name have no meaning, and before 2026-08-16 the dictionary underneath threw its own message at the developer instead. **`zorder` brings a control to the front of its container or sends it to the back** through MSForms' own ZOrder, which is a METHOD and reachable no other way. Two things measured before it was built: the Controls collection this route walks is NOT in z-order and does not move when ZOrder is called, so nothing here can read a control's depth back; and ZOrder itself works, proved on the running form by two overlapping labels swapping which was on top. So the read side of this one is `capture` of a running form, and nothing else. **`liveness` runs the check that catches an edit made OUTSIDE this product's funnel** and answers which open designer tabs it re-projected. Every other designer route funnels through a re-projection, so this is the only way a probe can drive the path a native-designer edit takes - and a SYNC IMPORT of a `.form` is a real outside edit of the same shape, which is what the suite uses. In the product it rides the window events that already fire (a window appearing or going, never one moving) with a half-second floor between checks; the key is each control's name and bounds, measured at 49 wrappers and 3ms against a full projection's 120 and 6ms. It catches what a hand does in a designer - add, remove, rename, move, resize - and deliberately not a property changed without one of those. Called through the route the floor is stood down, because a caller asking the question is not to be told the answer was worked out half a second ago. **`baseline` answers what the SAVED workbook records as changed**, per control path (`Options.PickGround`), read out of band from the file on disk - the `.xlsm` is a ZIP, `xl/vbaProject.bin` inside it is a compound file, and MSForms writes a PropMask naming every property that is not the file format default ([MS-OFORMS] 2.2.10). No export, nothing written, and no COM crossing into the design. `saved` is false when there is nothing on disk to read - a workbook never saved, or a form added since the last save - and the projection then compares against a bare coclass as it always did. READ THE ANSWER KNOWING WHAT A SET BIT MEANS: the mask measures against the file format default, not against what the developer chose, so every control on a Tahoma form lists `FontName` because the file's default is MS Sans Serif, and every CheckBox, OptionButton and ToggleButton lists `BackColor` and `ForeColor` whatever was done to them. It is a short list of candidates worth asking about rather than a verdict. The route exists to be DIFFED: the same walk lives twice, here and in `tools\harness\saved-design.mjs`, both written against the specification rather than against each other, and pointing the two at one workbook is the check that catches a misreading its author would otherwise make identically in both |
| `menus` | GET | `path` | THE EDITOR'S OWN MENUS, with their ids, including every control the surface suppresses. `path` is the position chain, comma separated; absent means the menu bar. `suppressed` is this session's answer about each control, so the suppression table can be read back rather than inferred - it is a list of magic numbers, and before this the only way to learn one was to enumerate the bar by hand and write it into a comment. `path=900` is the composed **xlide** menu rather than one of the editor's, and answers each synthetic position with the id of the real control it resolves to, which is the one place that mapping is visible |
| `analysis` | GET | `rule`, `severity`, `module`, `line`, `code`, `project` | MACHINE-WIDE ANALYZER RULE SETTINGS, and the inline suppression, one route for both - the same mechanism the rules modal, the problems pane's menu and the lightbulb use, so the api cannot drift from the UI. Bare GET lists every rule the bundled analyzer ships: `allowed` is the override values the ANALYZER permits (warning and information rules take `off`; error rules take at most `warning`, and only where marked downgrade-safe - most mirror a VBE compile failure and take nothing), `override` is what stands on this machine, `suppressionScopes` is where a suppression comment can aim (the inline writer below picks `disable-file` for module-scoped rules - a next-line directive cannot cover a finding anchored at (1,1), which is xlide_vscode#52), and `settingsPath` names the user-space settings.json the choices persist in. `rule=`+`severity=` changes one - `default` clears - refused in words when the analyzer would not honour it, because the engine silently ignores an illegal override and nothing would say why. `module=`+`line=`+`code=` writes the inline `' @xlide-analysis-disable-next-line` comment above that line instead (any open workbook's module, open in a tab or not), which travels with the code; `detail` reports what happened either way. Changes reanalyse every open workbook, so `problems` moves |
| `bars` | GET | `name` | WHERE ONE COMMAND LIVES, AND WHICH COPY ANSWERS. `command` reports one word - `currently disabled` - and a command is offered in several places at once: Reset is on the menu bar, on two toolbars and on three context menus. This surface hides every toolbar the editor came with, so that refusal used to be the opinion of whichever hidden copy the walk met first. `places` is every control carrying it with its bar, that bar's `barVisible`, its own `enabled`, and its `state` - -1 is a pressed toggle, which is how EXCEL'S DESIGN MODE is read: `bars?name=designMode` answering -1 means the host will not run anything, and Reset, Break and Step Out are greyed on every bar that carries them while the project still reports design. Pressing that toggle while stopped also discards the break; `enabledCount` of zero is the editor genuinely refusing. `mode` is the ACTIVE project's, read live (0 run, 1 break, 2 design), beside `publishedMode` - the word the page was last told - because the poll that publishes swallows its failures and the two can drift. `modeError` names why a live read failed. `name` takes a command name or a bare id |
| `project` | GET | `project` | what the VBA project CONTAINS: every component with its kind, line count, whether a pane is open on it, and for a CLASS its `predeclaredId`, plus the project's execution mode. The object model's own answer, read from inside - except `predeclaredId`, which the object model has no property for: it is `Attribute VB_PredeclaredId`, read out of the SAVED package on disk, and it decides whether a class's bare name is a value or a type (`Ticket.ChangeTest` compiles against the first and is `Variable not defined` against the second). Three states, and `null` is not `false`: null is a class the saved file cannot answer for - one added since the last save - and null is what keeps the analyzer silent, so this field is how a caller sees why a finding did or did not appear |
| `workbook` | POST | `action=close`, `project`, `saveChanges=0\|1` | CLOSES ONE OPEN WORKBOOK, the host's own File Close with the save question answered in the request. The one safe route for the gesture: the immediate window was the only other reach, and it is the one route that cannot survive it - its line runs as a procedure IN the active project, so closing that project's workbook tears the code out from under itself and the editor dies with it (measured 2026-08-28, one try, one dead Excel; #13). The evaluator refuses workbook-close and host-quit lines and points here. `saveChanges` is REQUIRED - the native close asks the developer this exact question, and a route that picked silently would lose work or write a file nobody asked for; this is a designated deviation from the prompt itself, answered in the request because that is what makes the route drivable. `saveChanges=1` on a never-saved workbook raises the host's own Save As, handled by the door's dialog watch like any dialog it raised. `project` takes every form the resolver knows - and an unsaved workbook resolves by the host's own name for it, "Book1", since #14 put its display on the same axis as saved ones |
| `outline` | GET | `module`, `project` | a module's procedures, from the analyzer: name, kind, line. For asserting on shape without parsing the text again in the caller |
| `pane` | POST | `action=open\|close\|closeNative`, `module`, `project`, `answer=save\|discard`, `face=design` | opens or closes a module's TAB. A close goes through the same gate the tab's own X uses, so unwritten edits raise the question unless `answer` settles it. An open answers `error` when nothing carries that name; it used to answer ok and do nothing. **A close answers `{closed, detail, awaiting}`** for the same reason: a save that would not save, a revert the module refused, and a confirm now standing on screen all left the tab where it was and all replied ok. `awaiting: "confirm"` is the third answer, neither success nor failure - answer it with `act answerCloseConfirm`. **`face=design` opens or closes a FORM's designer tab** - the markup-beside-visual tab - through the same method the page's own gesture uses. The designer tabs are PRODUCT STATE rather than a mirror of the host's pane list: the native designer window stays down on purpose (a live one summons the Toolbox, and a save while one exists restores it on open), so these tabs exist only in this product and `ui.workspace` reports them with `face: "design"`. A non-form is refused; closing a designer face never asks about unwritten edits, because unapplied markup is the page's own state and the page asks its own question. `closeNative` is the OTHER direction and **a designated deviation**: it closes the HOST's own pane window through the editor's pane list, which is the developer's click on the native child's close box - the direction the 2026-08-04 dead-tab defect lived in, where a HIDDEN pane's close fired no Changed and the strip kept the tab. The native close box asks no unwritten-edits question (that question belongs to the page's tab X), so none is asked; the tab disappears because the tracker notices the window go. Until this existed the host-originated close could only be produced through `Application.VBE` from outside, which project trust gates |
| `settings` | GET/POST | any setting name | the developer's settings, and a POST changes only what it names. the page's own update takes the whole object. `syncEngine=xlide\|builtIn` chooses which planner decides an import or export. **`insertOptionExplicit`** (default true) starts every module xlide creates - Insert menu or `component?action=add` - with `Option Explicit`, composing with the VBE's own Require Variable Declaration (whoever gets there first wins, the line appears once); a change-log restore never seeds, it recreates what was recorded. **`designerSnap`** is what the form designer's canvas snaps POINTER gestures to - `grid`, `objects` or `off`, one or the other and never both, because two snapping systems that disagree give a control two right answers. `grid` ships, at the `designerGridSize` spacing (six points, the editor's own Align Controls to Grid); `objects` lines up with the edges and centres of the neighbours sharing a container and draws a guide where it lands. An arrow key moves a single point whatever it says, and holding Alt escapes whichever mode is on - `act designerDrag ... alt 1` holds it for a whole synthetic gesture. A suite that changes it is changing the developer's own file, so it puts it back in its `finally` |
| `palette` | POST | `action=hide` | puts the Object Browser palette away, the way its own close box does: its WM_CLOSE handler HIDES with state intact, so the next summons (the `objectBrowser` command, which means summon and not toggle) presents the same page. Answers `{did, detail, visible}`; `did` is false when no palette exists yet. Existed from 2026-08-12, because every probe that summoned the palette either left it standing in front of the next probe or reached for a window message from outside the process - and the summoning command cannot put it away |
| `session` | POST | `action=cancelledShutdown` | drives a shutdown that is begun and then cancelled - `OnBeginShutdown` WITHOUT a real process exit - which stops the session and arms the watchdog, and because the editor frame stays standing the watchdog revives the session, exactly as it does when a developer cancels Excel's save prompt. That was the 2026-08-02 field failure (the add-in came back DEAD inside a living Excel) and had no test, because reaching it meant closing Excel by hand. **The reply arrives BEFORE the teardown** - it must, or it would ride the ApiServer that `Stop()` disposes - so after it this port goes dead; the revived session writes a fresh port and `startedAt` into `xlide-api-<pid>.json`, and the client reconnects by re-reading it (`discover()` / `open({pid})`). Debug only |
| `frame` | POST | `action=close\|show` | the editor window itself: the one pair of gestures every developer makes daily and no route could produce. `close` POSTS the developer's own X click (`SC_CLOSE` delivered by the pump after the reply), so the editor runs its whole close path - the hide, the palette follow, the placement retreat - outside the request; the reply's `visible` still reads true and **the outcome is observed on `state.frameVisible`**, the rule every posted effect on this door lives by. `show` reaches the same end state as Excel's Developer > Visual Basic button, synchronously, so its reply is the outcome. Closing the editor shipped a crash once (lesson 27); until this route the only automated driver was a probe sending window messages from outside |
| `undoRename` | POST | | puts the last rename back: every module it touched and the component's old name. The editor's own undo cannot, because a rename spans modules and the undo stack is per model. Answers `{undone, from, to, modules, stopped}`, where `stopped` is the half that matters: an undo can restore four modules and be refused the fifth, leaving the project in neither state. It answered a bare true until 2026-08-11, and the sentence saying which of those had happened went to the page under a request id this route invented, where no caller could read it. **A DESIGNATED DEVIATION**: it reaches the session's undo directly, so it proves the operation is reversible and NOT that the context-menu item works. Drive the menu item with `act editorAction id=xlide.undoRename` when that is the question |
| `breakpoints` | GET | | every recorded breakpoint as `{module, project, lines}`, and the project mode. **`project` is part of the identity**: two open workbooks can each hold a module of the same name, and the record was keyed by the name alone until 2026-08-08, so a breakpoint set in one was reported against the other and a run that should have stopped did not |
| `type` | POST | body = text, `waitMs` | types into the editor through its own KEYBOARD pipeline (`trigger("keyboard", "type")`), so smart Enter, comment continuation and auto-indent all run. A `\n` is sent as a real Enter, because that keystroke is what the block layout hangs off, and the text is typed in segments with a turn of the event loop between them - smart Enter defers its work to a microtask, so a script that types a newline and the next line synchronously computes the continuation against a line that already has the next one on it, which is how this route reported comment continuation broken when it was not (2026-08-07). Setting the text instead would go around every handler that makes typing feel like anything. Use `act press` for a single Enter, Tab, Backspace, Delete or Escape |
` is an Enter, and each segment is given a turn of the loop - typing has gaps, and a script without them tests nothing |
| `mark` | POST | `text` | writes a labelled line into the shim log and answers the byte offset it landed at, so `log?since=` can return exactly the slice a step produced |
| `command` | POST | `name`, `keep` | runs an editor command by name (`VbeCommands.ForName`). `keep=1` exempts any dialog it opens from the guard below. **`ran` is the editor's answer, not the door's**: it used to be the constant true, because the session method that executes a command computed the answer and returned void, so pressing Step Over outside break mode was reported as a step. `detail` says why a false came back - `not present in this host`, `currently disabled`, or the exception's name - because those want different responses from a caller. **`run` with a designer tab holding the active slot runs THE FORM** - the editor's own F5 with a designer selected. The editor POSTS the action, so the reply comes promptly and the form stands a beat later; watch and close it through `userform`; the designer window the run was aimed over stands behind the running form, as the native editor leaves it, and goes down once the run ends. **`save` with a designer tab holding the active slot applies the tab's document FIRST** - the designer's FlushEdits; the document lives in the page, so the host asks it to apply and the page calls back for the raw save - which is the route the real Ctrl+S takes, the key being a host accelerator the page never sees |
| `caret` | POST | `line`, `column`, `module`, `project` | puts the caret there, navigating first when a module is named. **A navigation that finds no pane is an `error` now**, not an ok: it left the caret in the module already shown, and a caller's next Run, Step or breakpoint then landed there instead |
| `breakpoint` | POST | `module`, `line`, `project`, `state=on\|off` | goes to the line and sets, clears, or toggles a breakpoint. `ran` is the record READ BACK after the toggle, so a line the editor will not carry one on answers false and says so; it used to answer the state that was asked for. A navigation that did not arrive answers `error` rather than toggling against whichever module was already shown. A `state` this route does not know is now REFUSED by name rather than falling into the absent-means-toggle default - `state=set` is a reasonable guess and is not the vocabulary, and asking twice for a breakpoint to BE set used to leave it clear with nothing saying the word had not been understood. Whether a line can carry one is judged against the WHOLE module, not the line: a line cannot say it sits inside a `Type` or an `Enum`, and every member and field used to be offered, refused by the editor with a modal, and recorded here as set |
| `immediate` | POST | `text`, `waitMs` | evaluates a line in the Immediate window and answers WHAT IT CAME TO: the result text and whether it failed. It used to answer `{ran: true}` without waiting, so a caller learned that an evaluation had been ASKED FOR and nothing else, and what the expression evaluated to went only to the page. That is the reason this panel had a route and no suite: nothing could read what it said. The same rule the rest of this door follows, where `closeActive` reports whether the tab actually closed and `compile` answers errors as data. WITHOUT `text` it reads instead, answering the whole window as it stands. `ran: false` means the evaluation did not finish inside `waitMs` (default 15000); the line still went in |
| `placement` | POST | | forces a placement pass |

### Awaiting, rather than sleeping

`eval` awaits a promise. The browser's own `ExecuteScript` hands back `{}` for one, which
looks exactly like a page fault: every async probe written against this door returned an
empty object and was read as broken until the shape was recognised. The script is evaluated
inside a wrapper that stashes a pending promise on the page and returns a ticket, and the
ticket is collected on a poll until it settles. So this works:

```powershell
Invoke-RestMethod "$api/eval" -Method Post -Body '(async () => { await something(); return answer; })()'
```

`await` takes a predicate instead of a script and polls it IN the page until it is true,
answering `met` and `elapsedMs`. It replaces the caller-side poll loop, which costs a round
trip per tick and invites a `Start-Sleep` chosen by guess - the loops written during the
workspace work raced the thing they were watching more than once. The elapsed time is the
interesting half of a PASS: a condition met in 4ms was already true, one met in 4 seconds
arrived.

One trap is written into the implementation. The page's content policy exempts the browser's
own synchronous evaluation, but NOT a later callback: a waiter that evaluated its predicate
string inside `setTimeout` was refused ("unsafe-eval is not an allowed source") and reported
every condition as unmet. The predicate is compiled to a function once, up front, and the
timer calls the function.

### Seeing the arrangement

`layout` answers with the whole visible shape in one request: each dock section with its
group tree and each group's tabs, the editor groups with their module tabs, the sizes, the
open documents, and whether a drag is live. It exists because building the docking layout
needed a dozen ad-hoc `eval` measurements per question - and it earned itself the day it
landed, answering "why is the Problems pane on the left?" in one call. (It was a probe that
had dragged it there and not put it back.)

`reload` reloads the page and WAITS for it to come back, answering with how long that took,
the build stamp the page is now running, and whether that stamp is behind the bundle on
disk. The manual version - reload, sleep a guess, hope - was run a dozen times in one
afternoon, and a guess that is too short reports on the page that is going away.

`layout?reset=1` (POST) puts the arrangement back and waits for the page. A probe that drags
panes about is testing the right thing and leaving the wrong thing behind - the layout is
persistent state, and clearing its storage key does not undo what the page already holds in
memory.

### Seeing why the page looks like that

`inspect` answers with the elements a selector matches: their boxes, classes, whether they
are hidden, the computed value of any styles named, and - with `rules=1` - which CSS rules
claim those properties, spelled out.

```powershell
Invoke-RestMethod "$api/inspect?selector=.dock-split&styles=align-items,display&rules=1"
```

The rule list is the point. This page shares a document with a large bundled stylesheet, and
a structural class of ours once inherited `align-items: baseline` from an unrelated rule,
collapsing every pane to its tab strip's height. It read as a flex bug in our own code and
cost an hour; the loop that eventually found it - walk every stylesheet, keep the rules this
element matches - is now this route.

`console` answers with what the page said to itself. Only UNCAUGHT errors reach the shim log,
deliberately, because forwarding every line would drown it - so a handled `console.error` or
a warning is invisible without DevTools attached, which is exactly the situation during a
live test. The ring is installed at page ready (including a reload's), wraps rather than
replaces the console, and keeps the last 500 lines.

### Looking at one widget

`capture?selector=` crops the screenshot to an element. A whole frame is a large picture in
which a 54-pixel drop zone cannot be seen, and a surface built by reading numbers rather than
looking at it is built with one eye shut. `tools\harness\Get-Shot.ps1` wraps this and
converts to PNG, which is what most things that want to LOOK at the result can open:

```powershell
tools\harness\Get-Shot.ps1 -Selector '.drop-compass' -Out compass.png
```

One trap is recorded in the code. The page reports coordinates inside its own client area,
and the crop has to place that area inside the captured frame. The surface's overlay window
is NOT the document area it is a child of - the surface draws the menu bar and toolbar too,
so it is taller - and a first cut that used the parent landed tens of pixels high, returning
the toolbar when asked for a pane header.

### Numbers for what a developer feels

`bench` times a named scenario in the page and answers min, median, p95, max, and the raw
samples. The counters elsewhere say what the HOST spent; this says what the surface costs,
which is where the risk moved when the workspace learned to split and dock.

| `what` | measures |
| --- | --- |
| `tabswitch` | putting another open document's model on screen - the live-model claim, in milliseconds |
| `layout` | re-measuring every editor, which is what a splitter drag and every dock change costs |
| `type` | an edit applied to the model and the page's work to show it |

```powershell
Invoke-RestMethod "$api/bench?what=tabswitch&n=40"
```

Two arguments deserve their reasons.

`breakpoint` takes `state=on|off` because a bare toggle is not safe for a script: a retry
clears what the first call set, which cost a live run its breakpoint during this api's own
verification. Without the argument it still toggles, the way the key does.

`immediate` RUNS the line and answers what it came to, waiting up to `waitMs` (default 15000)
for the value or the line's own error - not scheduling it and returning, which is what an
earlier version did and this document went on describing after the behaviour changed
(2026-08-07). The wait is on the DOOR's thread, not the host's, precisely so a line that raises
a Compile-error box does not jam the connection: the door notices the box, reads its words as
the answer, and clears it. The one thing it cannot outwait is a line that stops at a REAL
breakpoint and sits there until the developer continues; that is the debugger working, and the
scratch-break recovery above is only for the editor's own error box.

## Reading a break

The round trip the debugger milestone needs, and what `Test-DebugApi.ps1` walks:

1. `POST module?name=X` with the code, so the module has something to run.
2. `POST breakpoint?module=X&line=N&state=on`.
3. `POST caret?module=X&line=M` with M inside the procedure to run.
4. `POST command?name=run`. Expect this request to TIME OUT: a run that reaches a
   breakpoint does not let the host thread answer until it gets there. The timeout is not a
   failure, and the next step is the real assertion.
5. Poll `state` until `debugMode` reads `break`.
6. `GET locals` and `GET watches`.
7. `POST command?name=reset`.

Step 3 is not optional and not obvious. Every editor command acts on the CARET, and the host
copies the surface's caret into the native pane before running one, so a Run with the caret
on line 1 does what the editor always does there: it opens the Macros dialog and waits for a
person. `revealLine` scrolls without moving the caret and cannot substitute.

Two ways NOT to start the run, both learned the hard way on 2026-08-06:

- `Application.Run` from a process you might kill. The call BLOCKS inside the break, and
  killing its caller while Excel is suspended in it takes Excel down. It was a background
  job with a `Remove-Job -Force`; Excel crashed and restarted itself.
- `Application.OnTime`, which is silently unreliable here: it needs Excel idle and the macro
  name resolvable at fire time, and a module written seconds earlier did not always qualify.
  It works often enough to look correct and fail a probe at random.

Locals and watches come from the ghost reader thread's published snapshots, so these routes
never touch the accessibility layer themselves and cannot disturb a break. That matters: an
out-of-process accessibility client that dumps a ghost palette during a break can reset the
project (lesson 33), which is exactly why these routes exist instead.

## Never blocked by a modal

A modal dialog owns the editor until somebody answers it, and the two worst evenings this
project has had both ended that way: an Add Watch dialog whose filler mis-parsed its
arguments, and a Macros dialog raised by a Run with the caret on line one. Both times the
editor simply stopped, and nothing could say why.

Four things now stand between a modal and a wedged session.

**It can be seen.** `dialogs` enumerates windows, which needs no host thread, so it answers
while every other route would time out. It returns each dialog's caption, **the text it
says**, and its buttons. The text matters: every VBA compile error wears the caption
"Microsoft Visual Basic for Applications", so a harness reading captions alone learns that
something is wrong and never what.

`doctor` names a standing dialog as a finding, because a session with one is not healthy
however well every other route answers.

**Do not use `heartbeatAgeMs` to detect one.** A VBA modal PUMPS messages, so the host thread
keeps completing poll ticks the whole time it is blocked: measured 2026-08-07, a compile error
stood for fourteen seconds with the heartbeat never above 140ms. What is standing is the only
evidence that something is standing.

**It is cleared automatically, if the door raised it.** Every request sweeps dialogs this door
is answerable for - including the routes that need no host thread, which is where the sweep
belongs, because `dialogs`, `dismiss` and `guard` are exactly what a caller reaches for while
something is stuck. It presses one SAFE button, and what counts as safe depends on what is
being asked:

- A **notice** - every button an acknowledgement (OK, Help, Close, Continue) - is answered with
  OK. It is reporting, not asking, so pressing it decides nothing. This case used to be
  missed: the policy was Cancel/Close/No only, so a compile error offering OK and Help matched
  nothing and stood with the host thread behind it.
- A **question** is only ever declined: Cancel, then No, then Close. Never OK, Yes, Save,
  Delete, or Run - a dialog nobody read must not be agreed with.

A request that means to open a dialog passes `keep=1` and what it opens is exempt.

**A harness can ask for more: `guard`.** While the guard is on, a NOTICE the door did NOT raise
is cleared too, and recorded in `cleared` so nothing is swallowed silently. This is off by
default and is never turned on by itself, because the attribution rule below is right for a
person at a keyboard and wrong for an unattended run: a compile error raised by an experiment
stood for six minutes with everything behind it (2026-08-07), and no route could say so.

A question is still never answered, guard or no guard.

**A dialog you opened is never touched.** Attribution is the whole safety property, and it
took three attempts. A snapshot taken as a request ends catches nothing, because the dialog
arrives microseconds after the command returns. Comparing against "whatever stood when the
door last looked" then cancelled a developer's own Add Watch between requests - the one
outcome worth avoiding entirely. What works: after each request a pool thread looks again at
250ms, 750ms, and 1750ms, and the door owns only what appears in that window. A dialog that
opens while the door is idle is nobody's business but yours, however long it stands.

Verified in `Test-DebugApi.ps1`: a Run with a bad caret raises the Macros dialog, the door
sees it with its six buttons, clears it on the next request, and the heartbeat returns - and
separately, an Add Watch opened outside the api survives repeated api traffic untouched.

The mechanism behind all of this, and the rules for opening a modal at all, are in
[working-with-modals.md](working-with-modals.md).

## Stating expectations, capturing moments, replaying sessions

`assert` takes a named claim and waits for it: `stopped`, `running`, `surfaceReady`,
`shownModule`, `nativeModule`, `noDialogs`, `localsHas`, `watchHas`, `problemFree`,
`responsive`. It answers with what it SAW as well as whether the claim held, which is the half
a bare false leaves out - `{"held":false,"claim":"shownModule","expected":"NoSuchModule","saw":"BrokenModule"}`
diagnoses itself.

`shownModule` and `nativeModule` are deliberately two claims. The first asks the surface what it
believes it is showing, which is this session's own record; the second reads the host's
ActiveCodePane, which is what Run, Step and ToggleBreakpoint actually act on. Asking the first
about where a command will land is asking our record to confirm our record.

**A NAME THAT IS NOT A CLAIM IS AN ERROR**, answered immediately. It used to be polled like a
claim that had yet to come true, so a typo cost the full ten second timeout and then answered in
the same shape a real failure answers in, which reads as the product being broken.

`journal` captures state, standing dialogs, the counters, the recent log, and the recent page
traffic in ONE request. Evidence gathered request by request describes several different
moments, and the interesting one is usually the one that has already passed.

`history` hands back the door's last 300 state-changing requests - the poll routes are left out
so they do not drown the transcript - plus a runnable script of them. After an investigation by
hand, the useful sequence is normally reconstructed from a scrollback and gets a step wrong; this
turns a bug found by hand into a probe by copying.

## A note on the heartbeat

`heartbeatAgeMs` is the age of the host thread's last periodic tick, and it means "blocked"
only while something should be ticking. An idle editor stops polling by design, so a quiet
session legitimately shows a large age - `doctor` therefore only reports it as a finding when
polling is expected, after the first version of that check cried wolf on a quiet editor.

## Awaiting instead of sleeping

`log?match=...&waitMs=15000` returns the moment a matching line is written, or when the wait
expires. That removes the whole sleep-and-hope class of harness bug: a probe that slept a
guessed interval is slow when the guess is generous and flaky when it is not, and it can
never say whether the thing it wanted actually happened. Measured: a wait for a publish line
returned in 3.4 seconds, when the event occurred, not at the 15 second limit.

```js
const api = await open({ workbook: "scratch.xlsm" });
await api.command("save");
await api.waitForLog("modules: publish", { timeout: 10000 });
```

## When the page itself throws

A page exception is invisible without a DevTools client attached, which is never the case
during a live test: the surface misbehaves, the shim log says nothing, and all anyone has is
a description of symptoms. The page now forwards `error` and `unhandledrejection` to the host,
where they appear in the log as `page: page error: ...` with the message, the source
location, and the stack. Errors only - ordinary console noise would drown the log it is
meant to help.

## The DevTools door

Debug builds also start the browser's own DevTools protocol on a per-process port,
announced as `devtoolsPort` in the discovery file. That drives the LIVE page semantically:
real events on real elements, including double clicks, which pixel messages cannot produce.
`tools\harness\objbrowser-live-probe.mjs` is the reference client.

The port is picked per process rather than fixed. A fixed one (9333, in the first landing)
either collides between two Excels or, worse, lets them share a browser cluster and mix
their targets on one socket.

## Using it

```powershell
# Every live instance, each with its host and its agent URL
node tools\harness\xlide-api.mjs instances

# The api explains itself: the front door, the route table, the recipes
node tools\harness\xlide-api.mjs agent
node tools\harness\xlide-api.mjs agentRoutes

# What the language service knows
node tools\harness\xlide-api.mjs model Worksheet
node tools\harness\xlide-api.mjs analyzer

# One instance, chosen by workbook
node tools\harness\xlide-api.mjs --workbook scratch.xlsm state
node tools\harness\xlide-api.mjs --workbook scratch.xlsm locals

# The standing regression probe: every route plus the break round trip
powershell -File tools\harness\Test-DebugApi.ps1
```

```js
import { open } from "./tools/harness/xlide-api.mjs";

const api = await open({ workbook: "scratch.xlsm" });
await api.writeModule("CleanModule", source);
await api.breakpoint("CleanModule", 8, { state: "on" });
// start the macro, then:
await api.waitFor((s) => s.debugMode === "break");
console.log(await api.locals());
```

## History

This is the second landing. The first (branch `post-v010-experiments`, 2026-08-05) was
rolled back with the rest of that branch after a crash storm during break-mode work. The
crashes were not the api: they were an undersized VARIANT in the accessibility interop,
root-caused and fixed on 2026-08-05
([locals-break-investigation.md](locals-break-investigation.md)). What changed in this
landing, beyond the fix underneath it:

- locals and watches read the ghost reader thread's snapshots instead of mirrored fields
  the session had to maintain.
- the DevTools port and the discovery file are per process, for several Excels at once.
- dead instances' discovery files are swept at session start.
- `breakpoint` gained `state=on|off`, and `caret` is new: without it nothing outside the
  page can aim a Run at a procedure.
- a client library (`xlide-api.mjs`) and a standing probe (`Test-DebugApi.ps1`, 30 checks)
  ship with it.
- the modal guard above, `dialogs`, `dismiss`, `eval`, `doctor`, `perf`, `journal`,
  `history`, `assert`, the log wait, the host heartbeat, and page-error forwarding are all
  new in this landing; none of them existed on the branch.
