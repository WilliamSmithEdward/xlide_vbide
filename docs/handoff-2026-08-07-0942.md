# Handoff — 2026-08-07, 09:42 local (UTC-07:00)

Written at the end of the session that took the parity goal from an audit to six shipped
capabilities. The standing handoff is [handoff.md](handoff.md); this one is a point-in-time
record of what changed, what is open, and what the session cost to learn.

Head is `df53272`. Twenty-four commits from `f6c5fbd`. CI green. Dev build: page **67**.

---

## 1. What shipped

The parity goal in handoff.md section 9 is complete, and grew a sixth item on the way.

| Capability | Where it is answered | Verified |
| --- | --- | --- |
| Quick fixes | `codeAction` → `registerCodeActionProvider` | live host |
| Semantic colouring | `semanticTokens` → document tokens provider | live host |
| Go to definition | `definition` → `registerDefinitionProvider` | live host, cross-module |
| Find references | `references` → xlide's own results modal | live host, closed modules |
| Rename symbol | `rename` → `registerRenameProvider` | live host, closed modules |
| Rename module | `renameModule` → explorer and the rename box | live host, incl. document modules |

Everything above was read back **out of the VBA project through the object model**, not taken
from the editor's own report of what it had done. That distinction found two of the session's
real defects.

Alongside them:

- **Resize** got 82% cheaper per gesture: 1,298 pane refreshes down to 236 over 60 size steps.
- **Right-edge flicker** on resize is gone.
- **The tab strip** scrolls by edges rather than a native scrollbar, sharing one implementation
  and one appearance with the command strip.
- **The gate** fails when the packaged engine is older than the engine sources.

---

## 2. Open, in the order I would take them

> **Closed since this was written.** 2a and 2b below are both fixed; the account of each is kept
> because what it cost to find is the part worth keeping. See [section 6](#6-what-closed-2a-and-2b)
> for what was actually wrong and how it reads now.

### 2a. Peek Definition jumps instead of peeking — FIXED

The one thing left broken, and it is a **regression from this session**, not a pre-existing gap.

`provideDefinition` navigates through the host when the target is in another module, because the
standalone editor cannot open a model it is not showing. That fixed cross-module Go to Definition
— and broke Peek, because both commands go through the same provider. A provider that moves the
caret is answering a question by acting on it, which is the wrong shape and was the wrong fix.

The right seam is `monaco.editor.registerEditorOpener`, which the editor offers for exactly this.
**It was tried and it did not work**: with the opener registered and the provider pure, neither
peek nor reveal navigated at all — two broken things instead of one. That attempt was reverted
(the revert is the working tree at `df53272`; the attempt itself is not committed).

The current state is a deliberate choice: cross-module reveal WORKS, peek JUMPS. A broken jump
was judged worse than a peek that jumps. That is reversible if the judgement is wrong.

**What the next attempt needs**, and what makes it different from the last two: read
`StandaloneCodeEditorService.openCodeEditor` in
`ui/editor/node_modules/monaco-editor/esm/vs/editor/standalone/browser/standaloneServices.js`
and find what it checks before consulting registered openers. Both failed attempts guessed at
wiring. Neither read that function.

### 2b. Go to Definition on the declaration does nothing — FIXED

Invoked where the caret is already at the definition, the editor runs its alternative definition
command. That defaulted to peeking references — which showed **3** references where xlide's list
shows **4**, the missing one being in a module with no tab. It is now pointed at
`xlide.showReferences` instead.

The command works when invoked by that id, and the option holds that id, but the fallback never
fires — so today the answer is that nothing happens. Better than a window showing three of four,
and not what was intended.

### 2b-bis. Breakpoints are bookkept by module name alone

Found 2026-08-07 while getting break mode going with two workbooks open, and NOT fixed.

`_breakpoints` in `AddInSession` is a `Dictionary<string, SortedSet<int>>` keyed by the module
NAME. Two open workbooks can each hold a `Helpers`, so their breakpoints share an entry: setting
one in the twin's `Helpers` reports against the fixture's, and a run that should stop does not.
That is the same class of defect as the navigation one fixed the same day, in a different table.

The fix is to key it by (module, project) the way documents and tabs already are, which touches
about six call sites. Nothing about single-workbook use is affected, which is why it has waited.

### 2b-ter. The host's idea of the shown module can drift from the page's

Observed 2026-08-07 with two workbooks open, and NOT fixed.

`/state` reported `shownModule: null` and `shownProject: renamefixture.xlsm` while the page had
`TwinCaller/TwinFixture.xlsm` as its active tab. So the host had lost the module entirely and kept
a project belonging to the other workbook. It does not reproduce every time: the same sequence
later reported both correctly.

That drift is what broke Ctrl+W, because the key was answered host-side by closing
`_editorSurface.Module`. Ctrl+W no longer depends on it (the page decides which tab closes now),
so the symptom is gone, but the drift itself remains and anything else reading `shownModule` while
two workbooks are open inherits it.

### 2c. The rename ambiguity report is over-broad

`renameFor` returns an `ambiguous` list of uses it deliberately did not touch. It currently
includes the OTHER procedure's own declaration and its own calls, which are a different symbol
and not something a developer needs to decide about. Only the genuinely ambiguous bare call
belongs there. Nothing displays this list yet, so nothing is wrong on screen — but it is not fit
to show as it stands.

### 2d. Smaller, stated rather than hidden

- ~~**Rename cannot be undone.**~~ **DONE.** The edit spans modules and the editor's undo stack
  is per model, so Ctrl+Z would have been worse than nothing — it reverses one module's share
  and leaves the rest renamed. Undo Rename is a command of its own, over the same modules the
  rename touched, holding the text read out of each one immediately before writing.
- ~~**Rename resolves against the shown workbook.**~~ **TRIED, and it found something else.**
  Rename was right: with two workbooks each holding a `Helpers.Recalculate`, renaming either
  half of the twin left the other workbook byte-identical. What was wrong was NAVIGATION — the
  surface asked "am I already showing this?" by module name alone, so going to the other
  workbook's `Helpers` moved the native pane and left the surface where it was. Fixed; the
  comparison is a (module, workbook) pair now.
- **The loader spinner** occasionally appears during a resize. Ruled out: page reload (one
  overlay, one `ready`, `surfaceReady` true, nothing recreated). Not reproduced, so not fixed.
  The one path that would produce it without a reload is `HideLoader` returning early when
  `_handle == 0`, leaving `_loading` true forever. Unproven.
- **The diagnostic cascade** — one bad procedure header produces both `invalid-identifier-start`
  and a consequent `unmatched-block-closer`. This is in the SHARED analyzer, so xlide_vscode
  shows the same pair; fixing it here alone would make the two products disagree. The seam is
  `analyzeVbaModuleSource` in `xlide_vscode/src/vbaModuleAnalysis.ts`, which already filters at
  that point for transient-expression and suppression-directive cases.

  **FILED UPSTREAM and awaiting a fix there:**
  [xlide_vscode#10](https://github.com/WilliamSmithEdward/xlide_vscode/issues/10). Do not patch
  it from this repository — an analyzer fixed on one side only makes the two products disagree
  about the same source, which is worse than the noise because the disagreement is invisible
  until somebody compares them (the developer, 2026-08-07). The rename rules this repository
  settled on are written up for the same reason in
  [xlide_vscode#9](https://github.com/WilliamSmithEdward/xlide_vscode/issues/9).

---

## 3. What this session cost to learn

Recorded because each of these was paid for once and should not be paid for twice.

### The engine that runs is not the engine that was built

The add-in launches `engine\dist\xlide-engine.exe`. `npm run build` writes only `engine.cjs`,
which nothing at runtime reads. Four new operations were built, smoke-tested, committed and
published while the executable answering the add-in was hours old and refused every one as
"Unknown method". Three features were reported as verified and were not.

Nothing caught it: the engine smoke test runs against the bundle, and the gate did not build the
engine at all. It surfaced from a live session's log. `tools\verify.ps1` now fails when any engine
source is newer than the executable.

### Editor registrations cover less than their names

Five now, all found the same way — the provider registers cleanly and is then asked nothing,
which looks exactly like a provider that answers nothing:

| Feature | What its register module leaves out |
| --- | --- |
| `suggest` | the controller |
| `semanticTokens` | the whole-document contribution (it imports only the viewport one) |
| `gotoSymbol` | F12, Shift+F12, the peek windows, and their menu entries |
| `codeAction` | complete, but registers via `registerAction2`, so `getAction` returns null |
| references peek | `StandaloneReferencesController`, in a module nothing imports |

The way to tell a missing registration from a broken provider is to watch a running editor and
see whether the request goes out at all. Two neighbouring facts: a code-action provider must pass
`providedCodeActionKinds` as the third argument or the editor gates Ctrl+. off, and
`semanticHighlighting.enabled` must be set outright because `StandaloneTheme` hardcodes that flag
off and no standalone theme can say yes.

### The variable nobody varied

Twice, in the same shape.

**Rename** was probed only from the DECLARATION. Asked from a call site, the member resolver
answers with every module declaring that name, and the rename went through all of them —
renaming a `BrokenModule.RunTotal` that shared nothing but a name. Fixed by anchoring every
rename at the declaration whichever site it was asked from, so the entry point that was already
correct is the only one there is. The smoke test now pins it as a property: a rename from a call
site and one from the declaration must reach the same modules.

**Go to Definition** was tested only where the definition was in the SAME module. That path works
and looked correct. Crossing modules — the entire point of the feature — was never tried until
the developer tried it.

### Measuring beats the plausible hypothesis

Three times the obvious suspect was innocent and cost time:

- Resize lag looked like WebView2's compositor resizing a surface. It cost **0 ms across 191
  calls** — the browser side already skips unchanged bounds. The real cost was a cheap-looking
  refresh running 21 times per size step.
- Right-edge flicker looked like `CS_HREDRAW | CS_VREDRAW`. That flag is the existing
  *mitigation*; removing it would have shown the old native editor instead of a flat band.
- "Excel's window resize is our fault" — `MainWindowHandle` on that process is the **VBE frame**,
  not Excel's. Targeting `XLMAIN` explicitly showed **zero** refreshes and zero placements.

### What the preview harness cannot test

The browser pane does not composite frames, so anything needing a rendering lifecycle silently
does nothing there: screenshots, `ResizeObserver` (a fresh one fires **zero** times, not even its
initial callback), smooth scrolling, and zone widgets. A plain DOM modal renders fine — which is
part of why xlide's own references list is testable where the editor's peek window is not.

---

## 4. The test fixture

```bash
tools\New-RenameFixture.ps1
```

Writes `artifacts\fixtures\RenameFixture.xlsm` (gitignored, rebuilt on demand). Nine modules,
each labelled in its own comments with what to try and what must NOT move. `Consumer` is left
CLOSED and `Watcher` OPEN deliberately — the module with no tab is the one that gets silently
missed, and the two are reached by different code paths.

It found a defect on its first run: a comment ending in a full stop read as a qualified reference,
so `IShape.` was rewritten while `Helpers,` and `Widget,` beside it were not. Only code counts
now — strings end at their closing quote, doubled quotes being an escape, and comments run from
the first apostrophe outside a string.

Note `Circle` is not available as a module name; the Excel object library owns it and VBA refuses
with a bare HRESULT. The fixture uses `RoundShape`, and the script names the refused name rather
than passing the number along.

---

## 5. Working notes

- Publish Debug after every change: `dotnet publish src\Xlide.Vbe.Shim\Xlide.Vbe.Shim.csproj -c
  Debug -r win-x64`, with `DOTNET_ROOT` at `%LOCALAPPDATA%\Microsoft\dotnet` and
  `%ProgramFiles(x86)%\Microsoft Visual Studio\Installer` on PATH.
- Engine changes need `npm run package`, not `npm run build`.
- Excel holds both the shim DLL and the engine executable; close it first.
- `tools\verify.ps1 -Quick` before committing. Nine steps.
- The debug api discovery file is `%LOCALAPPDATA%\xlide_vbide\debug-api-<pid>.json`; routes
  `/doctor /state /stats /module /eval`. `/stats` now reports `windowEvents`, `refreshPasses`,
  `refreshTotalMs`, `refreshMaxMs`, `overlayMs`, `browserMs` and `browserCalls`.
- Those millisecond figures come from `Environment.TickCount64`, ~15.6 ms granularity. The counts
  are exact; the times are indicative. Move them to `Stopwatch.GetTimestamp()` before anyone
  optimises against them.
- `/eval` answers are encoded **twice** — the browser returns a script's result as JSON, and the
  door carries that as a JSON field. One parse leaves a string that reads as an object right up
  until every property of it is undefined, which is a probe reporting `false` for something that
  worked. Unwrap until it stops being a string.

---

## 6. What closed 2a and 2b

Added 2026-08-07, 11:20 local, after both were run against a live editor.

### Peek Definition jumped because the provider moved the caret

Not a wiring problem. `provideDefinition` called `bridge.navigateTo` for a target in another
module, and every command that asks about a definition — F12, Ctrl+click, Shift+F2 and Peek —
comes through that one provider, which cannot tell which is asking. So peek asked, was taken to
the definition, and never opened.

It could not even have reported the answer afterwards. The editor wraps a symbol-navigation
request in an `EditorStateCancellationTokenSource(editor, Value | Position)` — moving the caret
while a provider is running **cancels the request that asked**, and `runEditorCommand` returns at
`cts.token.isCancellationRequested` before it ever reaches `_openInPeek`. The jump and the silence
were one act.

The provider is now pure and the going happens in `monaco.editor.registerEditorOpener`, which is
the seam the editor offers for a document it cannot open itself. Two things about that seam,
because the previous attempt at it failed and neither was known then:

- Handlers are stored with `LinkedList.unshift`, so the **last registered runs first**. Registering
  after boot is correct; order was never the problem.
- The wrapper requires the opener to resolve **`true`**. Anything else — `false`, `undefined`, a
  promise of nothing — reads as "not handled" and falls through to the built-in handler, which
  only ever answers for the model already attached to the asking editor and therefore fails. That
  is exactly the shape of "neither peek nor reveal navigated at all".

A location for a module with no tab is now returned rather than dropped, its URI built by
`docUriOf`, with the host location kept beside it so the opener can name the workbook to the host
the way the host spelled it. Dropping those was what left the provider no way to reach the module
except to go there itself.

Measured against the fixture, caret on `Helpers.Recalculate` in `Watcher`:

| | shown module after | peek widget | what it drew |
| --- | --- | --- | --- |
| Peek Definition | `Watcher` — stayed | yes | `Helpers`, 14 lines, one result row |
| Go to Definition | `Helpers` — crossed | none | caret at the declaration, 6:12 |

### The alternative definition command can only ever be a symbol-navigation command

`SymbolNavigationAction.runEditorCommand` looks the configured id up in
`SymbolNavigationAction._allSymbolNavigationCommands` — the map those actions add themselves to —
and runs it **as one of them**. `xlide.showReferences` is a plain command, so it was never in that
map and the fallback could not have fired whatever it was pointed at. It is now set to the empty
string, which is what it honestly does, and the command it was pointed at is gone.

### Go to References is off the menu, by not answering the question it needs

No reference provider is registered any more. Both `editor.action.goToReferences` and
`editor.action.referenceSearch.trigger` carry `EditorContextKeys.hasReferenceProvider` in their
precondition, and `SymbolNavigationAction._patchConfig` copies a precondition into the menu item's
`when` clause — so with no provider there is no entry to draw and no keybinding to lose Shift+F12
to. Nothing was removed from a registry; the gate was simply left shut.

Read off a real right-click, dispatched through the browser's input layer (synthesised mouse
events do not reach the editor's own mouse handling, and the menu renders inside a **shadow root**,
so a document-only query finds nothing and reads as an empty menu):

> Go to Definition · Find All References · Peek · Rename Symbol · Change All Occurrences ·
> Format Document · Run Sub/UserForm · Run To Cursor · Toggle Breakpoint · Last Position ·
> Toggle Bookmark · Cut · Copy · Paste · Command Palette

Find All References, on Shift+F12, opens xlide's own list: 4 uses of `Recalculate` across
`Helpers`, `Consumer` and `Watcher` — `Consumer` being the module with no tab, which is the one
the editor's own window could not have drawn.
