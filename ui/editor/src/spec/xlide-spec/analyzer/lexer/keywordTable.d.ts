// Type surface of xlide_vscode/src/analyzer/lexer/keywordTable.ts, for the type-check only; the
// bundle carries the real implementation. See ../../vbaSmartEnter.d.ts for the why. Only what the
// page reads is declared.

/** The canonical capitalization of a VBA keyword, reserved or contextual, or undefined. */
export function canonicalKeyword(word: string): string | undefined;
